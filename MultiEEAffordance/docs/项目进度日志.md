# Multi-EE Affordance Dataset v0.1 进度日志

本文档用于记录数据集构建过程中的实质性进度。后续每完成一个重要阶段，都需要追加记录：完成日期时间、完成内容、产出文件、发现的问题和下一步计划。

## 2026-05-06 22:47 +08:00

### 完成内容

- 创建 `MultiEEAffordance/` 数据集原型目录结构。
- 建立原始数据、处理后数据、metadata、split 和工具脚本目录。
- 固定 v0.1 的四执行器通道顺序：
  - `gripper`
  - `suction`
  - `hook`
  - `dexterous_hand`

### 产出文件

- `taxonomy.yaml`
- `processed/metadata/samples_schema.md`
- `processed/metadata/samples.jsonl`
- `splits/train.txt`
- `splits/val.txt`
- `splits/test.txt`
- `splits/contrast_test.txt`

### 下一步

- 实现弱标签生成、可视化和数据检查脚本。

## 2026-05-06 23:10 +08:00

### 完成内容

- 实现点云、mask、metadata 的最小闭环工具。
- 将 `samples_schema.md` 改为中文说明。
- 补充 40 组强对比样本候选表。

### 产出文件

- `tools/generate_weak_masks.py`
- `tools/visualize_masks.py`
- `tools/check_dataset.py`
- `processed/metadata/contrast_samples_v0_1.jsonl`

### 下一步

- 补充真实数据接入流程和 3D AffordanceNet 转换脚本。

## 2026-05-07 10:57 +08:00

### 完成内容

- 识别 `raw/3d_affordancenet/` 下的三个压缩包：
  - `rotate.zip`
  - `full-shape.zip`
  - `partial.zip`
- 直接读取 `full-shape.zip` 中的 validation pkl，不全量解压大文件。
- 编写 3D AffordanceNet full-shape 转换脚本。
- 多轮修正弱映射规则，形成 v3 数据原型。
- v3 当前保留 46 个物体、61 条 object-task 样本。

### 产出文件

- `tools/convert_3d_affordancenet.py`
- `manifests/3d_affordancenet_full_shape_val_balanced_v3_manifest.jsonl`
- `processed/metadata/samples.jsonl`
- `processed/metadata/3d_affordancenet_full_shape_val_balanced_v3_summary.json`
- `docs/3D-AffordanceNet转换报告.md`

### 发现问题

- 3D AffordanceNet 原始 affordance 不是按执行器定义的，hook 和 suction 的弱标签需要保守处理。
- `open_pull` 不能从普通 `grasp` 直接推断。
- `pick_up/lift_carry` 不应对大件家具样本过度泛化。

### 下一步

- 做可视化检查和人工审查，找出弱标签规则错误。

## 2026-05-07 11:35 +08:00

### 完成内容

- 创建并验证 `multieeaffordance` Conda 可视化环境。
- 修正 matplotlib 导出 PNG 时的后端问题。
- 生成 HTML 和 PNG 可视化。

### 产出文件

- `tools/export_mask_html.py`
- `processed/visualizations/html_v3/index.html`
- `processed/visualizations/png_v3/door_open_pull.png`
- `processed/visualizations/png_v3/door_press_push.png`
- `processed/visualizations/png_v3/dishwasher_open_pull.png`
- `processed/visualizations/png_v3/earphone_pick_up.png`
- `docs/可视化检查报告.md`

### 下一步

- 建立人工审查表和网页审查工具。

## 2026-05-07 14:42 +08:00

### 完成内容

- 建立本地网页审查工具，支持在浏览器中查看单个样本并直接填写人工审查表单。
- 增加中文字段注解，说明样本级、执行器级、质量等级和备注如何填写。
- 根据需求回退掉原始 AffordanceNet candidate channel 显示，只保留 raw point cloud 与四执行器 mask 通道。

### 产出文件

- `tools/serve_review_app.py`
- `processed/metadata/manual_review_v0_1.csv`
- `docs/人工审查表字段说明.md`

### 下一步

- 完成 61 条样本人工审查，并根据审查结果生成 cleaned v0.1。

## 2026-05-07 19:49 +08:00

### 完成内容

- 61 条样本的人工审查已完成。
- 明确 `quality_after_review` 的判断口径：
  - `weak`：只能作为弱标签候选，仍需修正。
  - `checked`：人工看过且可作为当前版本训练/分析使用。
  - `verified`：需要更高可信度，通常要有多视角确认或精修结果支撑。
- 明确当前 suction/hook 的物理边界标准，避免把小吸盘或 hook 规则泛化到所有可接触表面。

### 产出文件

- `processed/metadata/manual_review_v0_1.csv`

### 下一步

- 将人工审查结果应用到 metadata、mask 和 splits，生成 checked/cleaned v0.1。

## 2026-05-07 20:13 +08:00

### 完成内容

- 编写并运行 `apply_manual_review.py`。
- 将 61 条人工审查结果应用到数据集中：
  - 保留 45 条样本。
  - 剔除 16 条样本。
  - 生成 66 条通道级 refine 队列。
- 对 `disable` / `not_applicable` 执行器通道做 mask 清零。
- 运行 `check_dataset.py`，确认 checked v0.1 的 45 条样本无 points/mask/metadata/split 错误。

### 产出文件

- `tools/apply_manual_review.py`
- `processed/metadata/samples_checked_v0_1.jsonl`
- `processed/masks_checked_v0_1/`
- `splits_checked_v0_1/`
- `processed/metadata/refine_queue_v0_1.csv`
- `processed/metadata/rejected_samples_v0_1.csv`
- `processed/metadata/manual_review_apply_summary_v0_1.json`

### 发现问题

- 当前 cleaned v0.1 只是样本级清洗和不可行通道清零；`refine` / `add_missing` 的逐点区域还没有真正补画。

### 下一步

- 生成人工审查总结文档，统计哪些弱规则错得最多。

## 2026-05-07 20:16 +08:00

### 完成内容

- 生成 `v0.1人工审查总结.md`。
- 统计出最高频错误：
  - 漏标最多，共 38 次。
  - hook 的 `missing_positive` 最突出，共 15 次。
  - dexterous_hand 的 `under_label` 较多，共 8 次。
  - suction 同时存在 `missing_positive` 与 `over_label`，说明平面面积、边缘、曲率规则不足。

### 产出文件

- `docs/v0.1人工审查总结.md`

### 下一步

- 设计 VLM 多视角辅助精修 pilot，先验证小样本流程，不全量跑。

## 2026-05-07 20:18 +08:00

### 完成内容

- 编写 VLM 多视角小试验设计文档。
- 编写三段脚本骨架：
  - 多视角渲染
  - 2D mask 回投 3D
  - 多视角投票融合
- 固定 VLM 提示词模板和四执行器定义。
- 修正投影脚本的点数推断逻辑：优先读取 `view_manifest.json` 中的 `num_points`，避免遮挡导致点数被低估。

### 产出文件

- `docs/VLM多视角试验设计.md`
- `tools/render_multiview.py`
- `tools/project_2d_masks_to_3d.py`
- `tools/fuse_multiview_masks.py`
- `vlm_prompt_templates.yaml`

### 下一步

- 从 refine 队列中挑选 5 到 10 条 pilot 样本，并生成多视角渲染。

## 2026-05-07 20:20 +08:00

### 完成内容

- 从 `refine_queue_v0_1.csv` 中挑选 8 条通道级 VLM pilot 样本。
- 覆盖执行器：
  - suction：2 条
  - hook：3 条
  - gripper：1 条
  - dexterous_hand：2 条
- 覆盖问题类型：
  - `over_label`
  - `needs_geometry_rule`
  - `wrong_region`
  - `missing_positive`
  - `under_label`
- 为 7 个唯一 object-task 样本生成 6 视角渲染，共 42 张 PNG，并保存 point-index/depth map。
- 修正 `render_multiview.py`：当 matplotlib 不可用时，使用标准库 PNG fallback，保证仍可导出图像。

### 产出文件

- `processed/metadata/vlm_pilot_samples_v0_1.csv`
- `processed/vlm_pilot/renders/*/view_manifest.json`
- `processed/vlm_pilot/renders/*/*_render.png`
- `processed/vlm_pilot/renders/*/*_point_index.npy`
- `processed/vlm_pilot/renders/*/*_depth.npy`

### 下一步

- 对 pilot 渲染图运行或手动模拟 VLM 2D 分割，得到每个视角的二值 mask。
- 用 `project_2d_masks_to_3d.py` 回投到点云。
- 用 `fuse_multiview_masks.py` 生成候选 3D mask。
- 将候选 mask 放回网页可视化工具中做人工复核。

## 2026-05-07 20:51 +08:00

### 完成内容

- 新增 OpenAI VLM pilot 调用脚本，用于把 6 视角渲染图转换为 per-view 2D mask。
- 新增批量候选构建脚本，用于把 VLM 2D mask 回投到 3D 点云、融合目标执行器通道，并生成网页复核用 samples JSONL。
- 新增 VLM pilot 运行说明文档，明确 VSCode 终端中的执行命令和输出路径。
- 尝试在 Codex 当前进程中调用 OpenAI API 进行 1 条 pilot 验证，但外部网络审批连续超时；为避免污染数据，没有生成伪 VLM mask。

### 产出文件

- `tools/run_openai_vlm_pilot.py`
- `tools/build_vlm_pilot_candidates.py`
- `docs/VLM试验运行手册.md`
- 更新 `docs/VLM多视角试验设计.md`

### 发现问题

- 当前 Codex shell 不等同于用户 VSCode 中激活的 `multieeaffordance` 环境。
- Codex 进程检测到 OpenAI SDK 和 `OPENAI_API_KEY` 变量名，但网络调用审批未通过，因此真实 VLM 调用需要用户在 VSCode 终端中执行。

### 下一步

- 用户在 VSCode 终端运行 `run_openai_vlm_pilot.py` 生成真实 VLM 2D masks。
- 运行 `build_vlm_pilot_candidates.py` 生成候选 3D masks。
- 用 `serve_review_app.py` 加载 `vlm_pilot_candidate_samples_v0_1.jsonl`，对候选 mask 进行网页复核。

## 2026-05-08 +08:00

### 完成内容

- 明确项目后续采用“本地 Codex 改代码 + GitHub 同步 + 远程服务器实际运行”的工作流。
- 记录远程服务器无法使用 Codex，因此所有重模型运行脚本必须能由用户在远程服务器手动执行。
- 将该约束写入独立工作流文档，作为后续开发和部署的长期约束。

### 产出文件

- `docs/本地Codex与远程服务器工作流.md`

### 下一步

- 后续补 Qwen3-VL + SAM2 pipeline 时，优先写远程服务器可执行的命令、配置文件和环境检查逻辑。
- 本地只做代码修改、轻量 dry-run、文档维护和网页审查，不默认承担大模型实际推理。

## 2026-05-08 10:41 +08:00

### 完成内容

- 根据远程服务器 4 张 24GB GPU 的资源情况，确定 Qwen3-VL-8B-Instruct + SAM2 的 pilot 运行策略。
- 新增远程服务器可执行的 `Qwen3-VL + SAM2` 脚本：
  - Qwen3-VL 逐视角读取渲染图。
  - Qwen3-VL 输出 box / positive point / negative point。
  - SAM2 根据这些 prompt 生成每个视角的 2D mask。
  - 输出目录兼容已有 `build_vlm_pilot_candidates.py`。
- 新增远程运行配置，默认使用 `Qwen/Qwen3-VL-8B-Instruct` 和 SAM2.1 large。
- 新增远程安装与运行说明文档，明确本地 GitHub 同步、远程 pull、远程运行、结果回传复核的流程。
- 修正 `本地Codex与远程服务器工作流.md` 中旧的配置文件名。
- 增加 `--validate-only` 模式，用于远程服务器在不加载 Qwen3-VL/SAM2 的情况下检查配置、pilot 表和多视角渲染文件。

### 产出文件

- `configs/qwen3vl_sam2_pilot.yaml`
- `tools/run_qwen3vl_sam2_pilot.py`
- `docs/Qwen3-VL+SAM2远程运行说明.md`
- `requirements-vlm.txt`
- 更新 `docs/VLM多视角试验设计.md`
- 更新 `docs/本地Codex与远程服务器工作流.md`

### 本地轻量检查

- `python -m py_compile MultiEEAffordance/tools/run_qwen3vl_sam2_pilot.py` 通过。
- `--validate-only --limit 1` 通过，已检查 1 条 pilot 的 6 个视角渲染文件。

### 远程运行建议

- 优先使用 `CUDA_VISIBLE_DEVICES=1,2`，避免占用截图中已有 Python 进程的 GPU0。
- 先运行 `--limit 1 --overwrite` 做 smoke test，再跑完整 8 条 pilot。
- 如果 Qwen3-VL 显存不足，再扩展到 `CUDA_VISIBLE_DEVICES=1,2,3` 或降低渲染图分辨率。

### 下一步

- 将本地代码提交并推送到 GitHub。
- 在远程服务器 `git pull` 后按 `docs/Qwen3-VL+SAM2远程运行说明.md` 安装环境并运行 1 条 pilot smoke test。
- smoke test 成功后运行完整 8 条 pilot，并回投融合生成候选 3D mask。

## 2026-05-08 路径兼容修复

### 完成内容

- 修复远程服务器运行 `run_qwen3vl_sam2_pilot.py --validate-only` 时读取到本地 Windows 绝对路径的问题。
- 新增通用路径工具 `path_utils.py`，支持相对路径、当前系统绝对路径和旧 manifest 中的 Windows 绝对路径。
- 修改 `render_multiview.py`，后续新生成的 `view_manifest.json` 默认写相对路径。
- 修改 Qwen3-VL+SAM2、OpenAI VLM、2D 回投、候选融合脚本，使其读取旧 manifest 时自动做跨机器路径映射。
- 新增 `normalize_render_manifests.py`，可批量把已有 render manifest 改成相对路径。
- 已将当前 7 个 pilot render manifest 从 `D:\VSCode\...` 改成 `processed/...` 相对路径。

### 产出文件

- `tools/path_utils.py`
- `tools/normalize_render_manifests.py`
- 更新 `tools/render_multiview.py`
- 更新 `tools/run_qwen3vl_sam2_pilot.py`
- 更新 `tools/run_openai_vlm_pilot.py`
- 更新 `tools/project_2d_masks_to_3d.py`
- 更新 `tools/build_vlm_pilot_candidates.py`
- 更新 `docs/Qwen3-VL+SAM2远程运行说明.md`
- 重写 `docs/本地Codex与远程服务器工作流.md`，明确路径约束和 GitHub/远程运行流程。

### 本地检查

- `run_qwen3vl_sam2_pilot.py --validate-only --limit 1` 通过。
- `normalize_render_manifests.py --dry-run` 显示当前 7 个 manifest 已无待修改路径。

### 下一步

- 本地将修复提交并推送到 GitHub。
- 远程服务器 `git pull` 后重新运行 `--validate-only`。

## 2026-05-11 选题思路整理

### 完成内容

- 新增选题详细思路报告，用于和 ChatGPT 或导师/师兄继续讨论研究方向。
- 报告不重复工程进度流水账，而是重点记录：
  - 当前研究题目的核心设定；
  - 为什么采用多通道输出而不是执行器条件输入；
  - 数据构建路线和阶段性结果；
  - 人工审查带来的关键认识；
  - Qwen3-VL + SAM2 在本题中的定位；
  - 当前困难、风险和防跑偏检查清单；
  - 后续可直接复制给 ChatGPT 的讨论提示。

### 产出文件

- `docs/选题思路跟进报告.md`

### 下一步

- 使用该报告继续讨论 novelty、标签边界、contrast_test 设计、VLM/SAM2 辅助标注定位和下一阶段优先级。

## 2026-05-11 Annotation Guideline 补充

### 完成内容

- 新增面向后续 VLM/SAM2 候选区域生成和人工标注审查的中文标注规范。
- 明确总体原则：本项目标注任务相关的可操作接触区域，不标所有可接触表面。
- 明确四类 canonical end-effector prototype 的机制和几何约束：
  - `gripper`: opposing contact + clamping force
  - `suction`: surface sealing + normal adhesion
  - `hook`: insertion + mechanical interlocking + pulling force
  - `dexterous_hand`: multi-finger contact + functional manipulation
- 按四类任务 `pick_up / lift_carry / open_pull / press_push` 写出各执行器标注重点。
- 增加跨通道混淆说明、人工审查 decision tree、VLM/SAM2 candidate mask provenance 和质量等级建议。

### 产出文件

- `docs/异构末端执行器标注规范.md`

### 下一步

- 后续 VLM/SAM2 输出和人工审查都应按该 guideline 执行。
- 若远程 pilot 结果出现系统性错误，需要回到该 guideline 中补充更细的 reject/uncertain 规则。

## 2026-05-12 09:45 +08:00 Qwen3-VL+SAM2 候选标注流程图与文档中文化

### 完成内容

- 新增 Qwen3-VL + SAM2 候选标注 pipeline 流程图文档，覆盖本地 Codex 改代码、GitHub 同步、远程服务器运行、多视角渲染、VLM 区域描述、SAM2 2D 分割、2D-to-3D 回投、多视角融合、网页人工审查和 cleaned metadata 更新。
- 将 `docs/` 下的 Markdown 文档文件名统一改为中文名，方便在 VSCode 和文件管理器中直接查看。
- 同步更新 README、脚本和配置中的文档引用，避免继续引用旧英文文档名。

### 产出文件

- `docs/Qwen3-VL+SAM2候选标注流程图.md`
- `docs/候选区域格式说明.md`
- `docs/3D-AffordanceNet转换报告.md`
- `docs/可视化检查报告.md`
- `docs/人工审查表字段说明.md`
- `docs/v0.1人工审查总结.md`
- `docs/VLM多视角试验设计.md`
- `docs/项目进度日志.md`
- `docs/VLM试验运行手册.md`
- `docs/Qwen3-VL+SAM2远程运行说明.md`
- `docs/本地Codex与远程服务器工作流.md`
- `docs/选题思路跟进报告.md`
- `docs/异构末端执行器标注规范.md`

### 本地检查

- 已检查 `docs/` 下 Markdown 文件列表，确认中文命名生效。
- 已检索旧英文文档名，当前 `.md`、`.py`、`.yaml` 中未发现残留引用。

### 下一步

- 将本轮文档调整推送到 GitHub，远程服务器 `git pull` 后继续按 `docs/Qwen3-VL+SAM2远程运行说明.md` 执行 Qwen3-VL + SAM2 pilot。
- pilot 生成候选 3D mask 后，回到本地网页审查工具，按 `docs/异构末端执行器标注规范.md` 做人工确认。

## 2026-05-12 10:15 +08:00 候选标注流程与导师汇报材料整理

### 完成内容

- 将 `docs/Qwen3-VL+SAM2候选标注流程图.md` 收敛为当前汇报需要的主流程，只保留从 pilot 样本读取到人工审查和 metadata 更新的候选标注链路。
- 新增导师汇报稿，说明当前 Qwen3-VL + SAM2 在项目中的定位、候选标注流程、2D-to-3D 回投逻辑、人工审查闭环和后续 pilot 计划。
- 在 `docs/异构末端执行器标注规范.md` 中新增“四类任务 × 四类末端执行器”汇报用总览表，便于快速说明标注边界。
- 更新 README 的 docs 文件列表，加入新生成的汇报稿文档。

### 产出文件

- `docs/Qwen3-VL+SAM2候选标注流程图.md`
- `docs/Qwen3-VL+SAM2候选标注与标注规范汇报稿.md`
- 更新 `docs/异构末端执行器标注规范.md`
- 更新 `README.md`

### 下一步

- 根据导师反馈，继续调整 candidate mask 的人工审查规则和 VLM/SAM2 prompt。
- 远程服务器完成 Qwen3-VL + SAM2 pilot 后，将候选 3D mask 导回本地网页审查工具复核。

## 2026-05-14 远程服务器 Hugging Face 离线加载修复

### 完成内容

- 根据远程服务器报错 `[Errno 101] Network is unreachable`，确认问题是服务器无法访问 Hugging Face，导致 `Qwen/Qwen3-VL-8B-Instruct` 的 `config.json` 无法在线下载。
- 修改 `run_qwen3vl_sam2_pilot.py`，支持在配置中指定本地 Qwen3-VL 模型目录：
  - `qwen3vl.model_path`
  - `qwen3vl.local_files_only`
  - `qwen3vl.cache_dir`
  - `qwen3vl.revision`
  - `qwen3vl.trust_remote_code`
- 增加本地模型目录检查：若 `model_path` 不存在或缺少 `config.json`，会给出明确报错。
- 更新 `configs/qwen3vl_sam2_pilot.yaml`，加入离线模型路径配置示例。
- 更新 `docs/Qwen3-VL+SAM2远程运行说明.md`，补充服务器无法访问 Hugging Face 时的模型下载、传输和配置方式。

### 本地检查

- `python -m py_compile MultiEEAffordance/tools/run_qwen3vl_sam2_pilot.py` 通过。
- 远程运行说明章节编号已检查。

### 下一步

- 本地提交并推送本次修复。
- 远程服务器 `git pull` 后，把 Qwen3-VL 模型目录放到 `MultiEEAffordance/external/models/Qwen3-VL-8B-Instruct/`，再把配置中的 `model_path` 打开并设置 `local_files_only: true`。

## 2026-05-15 SAM2 checkpoint 相对路径修复

### 完成内容

- 根据远程服务器报错 `No such file or directory: 'external/sam2/checkpoints/sam2.1_hiera_large.pt'`，确认问题是 SAM2 checkpoint 相对路径被按当前工作目录解析，而不是按 `--dataset-root MultiEEAffordance` 解析。
- 修改 `run_qwen3vl_sam2_pilot.py`，使 `sam2.checkpoint` 的相对路径统一基于 dataset root 解析。
- 增加 SAM2 checkpoint 存在性检查，若文件不存在，会输出解析后的绝对路径，便于定位文件结构问题。
- 更新 `configs/qwen3vl_sam2_pilot.yaml` 注释，明确 SAM2 checkpoint 相对路径解析规则。
- 更新 `docs/Qwen3-VL+SAM2远程运行说明.md`，补充 checkpoint 找不到时的检查命令和临时绝对路径解决方案。

### 下一步

- 本地提交并推送修复。
- 远程服务器 `git pull` 后可保持默认配置 `checkpoint: external/sam2/checkpoints/sam2.1_hiera_large.pt` 继续运行。
- 如果暂时不拉取新代码，可在服务器配置中把 `sam2.checkpoint` 临时改为绝对路径。

## 2026-05-15 Qwen3-VL 空候选输出排查增强

### 完成内容

- 根据远程完整 pilot 输出中所有 `positive_pixels` 均为 0 的现象，判断当前问题不是 API 配置问题，而是 Qwen3-VL 没有产生可用 SAM2 prompt，或输出坐标被解析/过滤成空 prompt。
- 修改 `run_qwen3vl_sam2_pilot.py`：
  - 保存 Qwen3-VL 原始生成文本到 `_raw_text`，便于远程排查模型到底输出了什么。
  - 兼容单个 `box` / `bbox` / `point` / `positive_point` 字段。
  - 兼容字典格式坐标，例如 `{x1,y1,x2,y2}` 和 `{x,y}`。
  - 兼容 0 到 1 的归一化坐标，并自动缩放到像素坐标。
  - 将 `feasible=true` 但没有 box/positive point 的情况视为不可分割，避免出现“feasible 但空 mask”的误导。
  - 强化 prompt 规则：若 `feasible=true`，必须至少给出一个 box 或 positive point。

### 本地检查

- `python -m py_compile MultiEEAffordance/tools/run_qwen3vl_sam2_pilot.py` 通过。

### 下一步

- 本地提交并推送修复，远程服务器 `git pull` 后重新跑 1 条 smoke test。
- 若仍全部为 0，需要优先查看 `processed/vlm_pilot/qwen3vl_sam2_responses/*/*_qwen3vl.json` 中的 `_raw_text` 和 `normalized_prompt`，判断是模型未识别点云渲染，还是 prompt 仍需调整。

## 2026-05-15 10:23 +08:00 VLM yaw 视角渲染与 pilot_005 单样本闭环

### 完成内容

- 根据远程 Qwen3-VL + SAM2 pilot 仍全部输出空 mask 的现象，判断当前主要瓶颈是输入渲染图可读性不足：旧 `front/back/left/right/top/iso` 视角和稀疏小点渲染很难让 VLM 判断把手、孔洞和可操作区域。
- 修改 `render_multiview.py`：
  - 默认视角改为一圈 yaw 角：`yaw000_elev20` 到 `yaw315_elev20`，每 45 度一个视角。
  - 默认图像尺寸改为 `768`。
  - 将回投用点半径与 VLM 视觉点半径解耦：`point_size=1` 用于 point-index map，`visual_point_size=4` 用于更连续的 VLM render。
  - 支持 `yawDDD_elevDD` 视角命名和解析。
  - 在 manifest 中记录 `point_size`。
- 新增 `tools/render_vlm_pilot_views.py`，可按 pilot 表批量重渲染 VLM 视角，并支持 `--pilot-id vlm_pilot_005` 单样本验证。
- 修改 `configs/qwen3vl_sam2_pilot.yaml`：
  - pilot views 改为 8 个 yaw 视角。
  - `pilot.image_size` 改为 `768`。
  - 打开 `segmentation.save_prompt_overlay: true`。
  - 增加前景约束参数，后处理 Qwen3-VL 的 box/point，使其落在真实点云前景上。
- 修改 `run_qwen3vl_sam2_pilot.py`：
  - prompt 增加点云渲染说明，明确深色背景为空、彩色像素为物体点。
  - prompt 增加 `visible_object_parts` 和 `target_region_description` 字段，让 Qwen3-VL 先做部件级理解再输出 SAM2 坐标。
  - 增加 foreground prompt refine：点和框若落到黑色背景，会被贴近膨胀后的真实点云前景；距离过远则丢弃。
  - 对 hook 通道优先使用前景正点，避免错误大框把 SAM2 引向物体主体。
- 修改 `build_vlm_pilot_candidates.py`：
  - 回投融合不再写死旧 6 视角，而是读取 `view_manifest.json` 中实际存在的视角。
  - 增加 `--pilot-id` 和 `--limit`，方便只对 `vlm_pilot_005` 做单样本闭环验证。
- 更新 `docs/Qwen3-VL+SAM2远程运行说明.md`，加入重新渲染 `vlm_pilot_005`、运行单样本 smoke test、检查 prompt overlay 和单样本回投融合的命令。

### 下一步

- 本地提交并推送。
- 远程服务器 `git pull` 后，先执行 `render_vlm_pilot_views.py --pilot-id vlm_pilot_005 --image-size 768 --point-size 1 --visual-point-size 4 --overwrite`。
- 只跑 `vlm_pilot_005`，观察 `positive_pixels` 和 `*_prompt_overlay.png`；若该典型 bag handle 样本仍为空，再继续调整渲染风格或 prompt。

## 2026-05-15 11:45 +08:00 hook 无源 mask 情况下的几何候选路线

### 完成内容

- 根据当前排查结果，确认 hook 通道不能假设源数据集中已有逐点 mask。对于 `Bag / lift_carry / hook` 这类样本，应先从点云几何中提出候选，再做语义选择和人工审查。
- 新增 `tools/generate_hook_candidates.py`：
  - 读取 pilot 样本和多视角 `point_index_map`。
  - 估计主物体的密集上边界，将主体上方的独立结构作为候选 A。
  - 同时生成较宽松的上部连通域候选 B 和 top-band fallback 候选 C。
  - 输出 `processed/vlm_pilot/hook_candidates/{pilot_id}/candidates.npz` 和 `candidate_manifest.json`。
- 新增 `tools/render_hook_candidate_overlay.py`：
  - 将候选 A/B/C 以不同颜色叠加到多视角渲染图上。
  - 供 Qwen3-VL 进行候选级选择，避免再次输出不稳定像素坐标。
- 新增 `tools/run_qwen3vl_candidate_selector.py`：
  - 只让 Qwen3-VL 从候选 A/B/C 中选择，不调用 SAM2，也不要求输出 box/point。
  - 输出逐视角 selection JSON 和 `combined_selection.json`。
- 新增 `tools/build_hook_candidate_mask.py`：
  - 将 Qwen3-VL 选中的候选写回 `[N,4]` mask 的 hook 通道。
  - 支持 `--selected-candidates A` 手动调试覆盖。
  - 输出 `processed/metadata/hook_candidate_samples_v0_1.jsonl`，可直接进入网页审查工具复核。
- 更新 `.gitignore`，避免将本地生成的 hook candidate 中间结果、overlay 和候选 mask 误提交到 GitHub。
- 更新 `docs/Qwen3-VL+SAM2远程运行说明.md`，新增 hook 几何候选路线和远程运行命令。

### 本地检查

- `python -m py_compile` 检查新增四个脚本通过。
- 已用本地 `vlm_pilot_005` 生成几何候选：
  - candidate A: 12 个点，主要对应包体上方提手结构。
  - candidate B: 126 个点，较宽松，包含部分包体上缘。
  - candidate C: 452 个点，是 broad top-band fallback，需要谨慎拒绝。
- 已生成候选 overlay，并人工查看 `yaw000_elev20_overlay.png`，候选 A 明显更接近 bag handle。
- 已用 `--selected-candidates A` 写回候选 hook mask，并通过：

```bash
python MultiEEAffordance/tools/check_dataset.py \
  --dataset-root MultiEEAffordance \
  --samples processed/metadata/hook_candidate_samples_v0_1.jsonl \
  --split-dir splits_hook_candidates
```

检查结果：`errors=0, warnings=0`。

### 下一步

- 本地提交并推送新增脚本和文档。
- 远程服务器 `git pull` 后，先按文档只跑 `vlm_pilot_005`：
  1. 重新渲染多视角点云。
  2. 生成 hook 几何候选。
  3. 渲染候选 overlay。
  4. 运行 Qwen3-VL 候选选择。
  5. 写回 hook candidate mask。
- 将候选结果同步回本地网页审查工具，确认 candidate A 是否确实符合 hook 的 insertion + mechanical interlocking + lift/carry 机制。

## 2026-05-15 14:07 +08:00 hook 候选图像可读性增强

### 完成内容

- 根据远程 Qwen3-VL 对 `vlm_pilot_005` 候选 overlay 全部输出空选择的结果，判断当前失败原因主要不是模型/API 问题，而是候选图中提手区域太小、太稀疏，VLM 在整物体视图中没有把红色稀疏弧线理解成可 hook 的把手结构。
- 修改 `tools/render_hook_candidate_overlay.py`：
  - 默认 overlay 点半径从 `5` 调小到 `3`，减少候选颜色遮挡主体形状。
  - 新增 selector panel 输出：每个视角额外生成 `{view}_selector.png`。
  - selector panel 左侧保留完整物体候选 overlay，右侧显示候选 A 附近的局部放大区域，让 VLM 同时看到全局位置和局部结构。
  - 右侧局部图不再裁入 legend，避免文字干扰 VLM 判断。
- 修改 `tools/run_qwen3vl_candidate_selector.py`：
  - 优先读取 `selector_path`，如果不存在再退回 `overlay_path`。
  - prompt 明确说明输入可能是“左侧全局图 + 右侧候选局部放大图”。
  - prompt 补充说明：点云中的真实细提手可能只表现为少量稀疏点，不应因为点少就直接判定为不可 hook。
  - prompt 强调候选 A 不是自动正例，但如果放大图中 A 位于包体上方的 handle/loop 上，应选择 A。

### 本地检查

- `python -m py_compile MultiEEAffordance/tools/render_hook_candidate_overlay.py MultiEEAffordance/tools/run_qwen3vl_candidate_selector.py` 通过。
- 本地重新生成 `vlm_pilot_005` selector 图，已查看 `yaw315_elev20_selector.png`：右侧放大图能明显看到红色 A 候选位于包体上方的弧形提手区域。
- `run_qwen3vl_candidate_selector.py --validate-only --pilot-id vlm_pilot_005` 通过，确认 selector 图路径可被脚本读取。

### 下一步

- 将本轮修改推送到 GitHub。
- 远程服务器 `git pull` 后重新执行：
  1. `render_hook_candidate_overlay.py --pilot-id vlm_pilot_005 --overwrite`
  2. `run_qwen3vl_candidate_selector.py --pilot-id vlm_pilot_005 --overwrite`
- 如果 Qwen3-VL 仍然不选 A，则不再继续强迫 VLM 做该样本判断，应采用 `build_hook_candidate_mask.py --selected-candidates A` 生成候选，交给人工审查确认。这个策略符合当前项目定位：VLM/SAM2 只是候选辅助，不是最终 ground truth 生成器。

## 2026-05-15 15:02 +08:00 初版 VLM/SAM2 pipeline 失败复盘与新版语义部件路线

### 失败复盘

- 远程 `vlm_pilot_005: Bag / lift_carry / hook` 实验显示，即使给出候选 overlay 和局部放大图，Qwen3-VL 仍多次输出 `no hookable structure visible`，没有选择包体上方的 handle 候选。
- 抽检 3D AffordanceNet full-shape 数据后确认：`full_shape_val_data.pkl` 中 2285 个物体全部为 `(2048, 3)` 点云；当前 processed 点云也全部为 `(2048, 3)`。因此 bag handle 只有十几个点不是渲染漏点，而是源点云细结构本身很稀疏。
- 初版失败的关键原因不是 Qwen3-VL 不能用于本项目，而是使用方式不合适：
  - 稀疏点云渲染图不像自然图像，VLM 难以稳定识别少量点构成的 handle / loop。
  - 让 Qwen3-VL 直接输出 box / point，本质上是在要求它做非自然图像上的精确像素标注，这是其弱项。
  - SAM2 面向连续图像区域，在稀疏点云图上容易无法形成稳定 mask。
  - 几何候选 A/B/C 可以发现上方结构，但几何规则本身没有 VLM 的语义知识，不能单独承担最终标注判断。
  - 3D AffordanceNet full-shape 没有明确 hook mask，不能通过已有标签直接修复 hook 通道。

### 新判断

VLM 的角色应从“像素坐标标注器”调整为“语义部件规划器”。新版 pipeline 的核心思想是：

```text
VLM 负责识别目标部件和解释任务-执行器机制；
GroundingDINO / Florence-2 负责根据部件文本定位 2D 区域；
SAM2 负责生成 2D mask；
point_index map 负责回投真实 3D 点；
几何规则负责校验；
人工审查负责最终确认。
```

### 完成内容

- 新增中文设计文档：`docs/VLM语义部件引导标注Pipeline.md`。
- 文档明确记录：
  - 初版 pipeline 失败原因；
  - 新版 pipeline 的模块分工；
  - dense render 与 point_index map 的双图像机制；
  - `vlm_pilot_005` 的预期 Qwen3-VL part planner 输出；
  - 后续输出文件结构和失败处理策略。

### 下一步

- 新增新版 pipeline 的脚本骨架：
  - `render_vlm_friendly_views.py`
  - `run_qwen3vl_part_planner.py`
  - `run_grounding_sam2.py`
  - `project_grounded_masks_to_3d.py`
  - `fuse_semantic_part_masks.py`
- 先只对 `vlm_pilot_005` 做单样本闭环，不全量处理 8 条 pilot。
- 该路线仍然只生成 candidate proposal，最终必须进入网页人工审查。

## 2026-05-15 15:20 +08:00 VLM 语义部件引导 pipeline 骨架落地

### 完成内容

- 新增 `tools/render_vlm_friendly_views.py`：
  - 从真实点云生成 dense render、silhouette、upper/target zoom panel。
  - 同时保存精确 `point_index_map`，用于后续 2D-to-3D 回投。
  - 明确区分“给 VLM 看的连续友好图”和“用于真实点回投的索引图”。
- 新增 `tools/run_qwen3vl_part_planner.py`：
  - 让 Qwen3-VL 只输出目标部件文本和 grounding queries，不再输出 box / point。
  - 支持 `--dry-run` 与 `--validate-only`，便于本地和远程分阶段检查。
  - 对 `vlm_pilot_005` 的 dry-run 会生成 `bag handle / top handle loop / hookable handle loop`。
- 新增 `tools/run_grounding_sam2.py`：
  - 建立开放词汇 grounding + SAM2 的接口骨架。
  - 当前支持 `manual-json`、`--dry-run`、`--box-mask-only` 和可选 `--run-sam2`。
  - GroundingDINO / Florence-2 先保留为明确 adapter 入口，后续在服务器环境中接入。
- 新增 `tools/project_grounded_masks_to_3d.py`：
  - 将 2D grounded mask 通过 `point_index_map` 回投到真实 3D 点。
  - 支持 `nearest-radius`，用于将 dense render 上的 mask 像素映射到邻近真实点。
- 新增 `tools/fuse_semantic_part_masks.py`：
  - 将多视角 3D votes 融合为 `[N,4]` candidate mask。
  - 输出 `processed/metadata/semantic_part_candidate_samples_v0_1.jsonl`，可直接进入网页人工审查。
- 更新 `.gitignore`，忽略 `processed/vlm_semantic_part/` 和 semantic-part candidate 中间结果。
- 更新 `docs/VLM语义部件引导标注Pipeline.md`，补充 `vlm_pilot_005` 单样本运行命令。

### 本地检查

- `python -m py_compile` 检查 5 个新增脚本通过。
- 已在本地跑通 `vlm_pilot_005` dry-run 闭环：
  1. `render_vlm_friendly_views.py` 成功生成 8 个 yaw 视角的 dense/silhouette/zoom panel。
  2. `run_qwen3vl_part_planner.py --dry-run` 成功生成语义部件计划。
  3. `run_grounding_sam2.py --dry-run --box-mask-only` 成功生成 2D box mask。
  4. `project_grounded_masks_to_3d.py` 成功回投到真实点云。
  5. `fuse_semantic_part_masks.py` 成功生成 semantic-part candidate mask，当前 dry-run 候选 hook 通道为 262 个点。
- 候选 metadata 检查通过：

```bash
python MultiEEAffordance/tools/check_dataset.py \
  --dataset-root MultiEEAffordance \
  --samples processed/metadata/semantic_part_candidate_samples_v0_1.jsonl \
  --split-dir splits_semantic_part_candidates
```

检查结果：`errors=0, warnings=0`。

### 注意事项

- 当前 dry-run 的 262 个点来自 zoom crop box mask，只用于验证数据流，不代表真实标注质量。
- 真正关键的下一步是远程运行 Qwen3-VL part planner，并接入 GroundingDINO 或 Florence-2，根据 `bag handle / top handle loop` 做开放词汇 2D 定位。
- Grounding/SAM2 结果仍然只能作为 candidate proposal，必须进入人工审查。

### 下一步

- 将本轮代码和文档推送到 GitHub。
- 远程服务器 `git pull` 后先运行：
  - `render_vlm_friendly_views.py --pilot-id vlm_pilot_005`
  - `run_qwen3vl_part_planner.py --pilot-id vlm_pilot_005`
- 如果 Qwen3-VL 能稳定输出 `bag handle / top handle loop`，再接 GroundingDINO / Florence-2 adapter；如果仍失败，则将该失败作为 VLM 对稀疏点云语义识别的 case 记录，并考虑加入人工部件文本输入。

## 2026-05-15 18:51 +08:00 Qwen3-VL 语义部件规划成功与 Florence-2 grounding 接入

### 完成内容

- 远程 `vlm_pilot_005` 结果显示，Qwen3-VL 在新版 part planner 中已能正确输出：
  - `visible_object_parts`: `bag body`, `bag handle`
  - `target_part_names`: `bag handle`
  - `grounding_queries`: `bag handle`
  - `mechanism_reason`: bag handle 适合 hook insertion / retention / lift_carry。
- 这说明新版思路的第一阶段成立：Qwen3-VL 更适合做语义部件规划，而不是直接输出像素坐标。
- 修改 `tools/run_grounding_sam2.py`：
  - 接入 `--backend florence2`。
  - 支持从配置读取 Florence-2 本地模型路径，服务器离线时可使用 `external/Florence-2-large`。
  - 使用 Florence-2 的 `<CAPTION_TO_PHRASE_GROUNDING>` 任务，把 `bag handle` 等 grounding query 转成 2D boxes。
  - 保留 `manual-json` 和 `dry-run` 作为兜底路径。
  - 支持 `--run-sam2` 将 Florence-2 box 输入 SAM2 生成 2D mask，也支持 `--box-mask-only` 做调试。
- 更新 `configs/qwen3vl_sam2_pilot.yaml`，新增 `florence2` 配置块：
  - `model_id: microsoft/Florence-2-large`
  - `model_path: external/Florence-2-large`
  - `local_files_only: true`
  - `task_prompt: <CAPTION_TO_PHRASE_GROUNDING>`
- 更新 `docs/VLM语义部件引导标注Pipeline.md`，补充 Florence-2 + SAM2 运行命令和离线模型目录要求。

### 下一步

- 远程服务器准备 Florence-2 权重，默认目录：

```text
MultiEEAffordance/external/Florence-2-large/
```

- 运行 Florence-2 + SAM2 grounding：

```bash
CUDA_VISIBLE_DEVICES=1,2 python MultiEEAffordance/tools/run_grounding_sam2.py \
  --dataset-root MultiEEAffordance \
  --config configs/qwen3vl_sam2_pilot.yaml \
  --pilot-id vlm_pilot_005 \
  --backend florence2 \
  --run-sam2 \
  --overwrite
```

- 若 Florence-2 对稀疏点云 dense render 找不到合理 box，则改用 `manual-json` 或测试 GroundingDINO adapter；但 Qwen3-VL 的语义部件输出应保留为稳定的上游规划结果。

## 2026-05-18 远程 Florence-2 + SAM2 跑通与 ROI 约束策略

### 完成内容

- 远程新环境 `multiee3dag_torch26` 中，Florence-2 + SAM2 已能完成 `vlm_pilot_005` 的 8 视角推理。
- 使用 `pytorch_model.bin` 后，模型加载日志中不再出现 `language_model.* MISSING`，说明权重加载问题已基本排除。
- 每个视角均输出 `boxes=2` 和非空 SAM2 mask，说明 open-vocabulary grounding + SAM2 分割链路已经实际跑通。
- 人工检查发现当前输出质量不合格：
  - Florence-2 将 `bag handle / handle loop` 定位为几乎整个 bag。
  - SAM2 在大框内分割出大面积包体，导致 mask 覆盖远超 hook 应标注的提手区域。

### 策略调整

- 当前问题不再是模型运行失败，而是 grounding 粒度太粗。
- 对于 `hook / handle` 类目标，候选区域应约束在可挂接的上方提手或环状结构附近，不能直接接受 Florence-2 的整物体大框。
- 修改 `tools/run_grounding_sam2.py`，新增 ROI 约束后处理：
  - 保留 Florence-2 原始 `raw_boxes`。
  - 对面积过大的 box，根据渲染阶段计算的 `zoom_crop_bbox` 进行裁剪。
  - 对 `bag / bag body / object / whole object` 等明显泛化标签进行拒绝或裁剪。
  - 记录 `refine_notes`，方便审查原始框如何被处理。
  - 若所有 box 被过滤，允许回退到 `zoom_crop_bbox` 作为候选 box。
- 修改 `configs/qwen3vl_sam2_pilot.yaml`，新增 `grounding_refine` 配置，用于控制大框裁剪、面积阈值和 fallback。

### 下一步

- 服务器拉取更新后重新运行 Florence-2 + SAM2。
- 重点检查 `*_boxes.json` 中的：
  - `raw_boxes`
  - `refine_notes`
  - 最终 `boxes`
  - `diagnostics.generated_text`
- 如果最终 box 已限制到上方 handle 区域，再继续执行 2D-to-3D 回投和多视角融合。
- 如果 ROI 约束后仍然 mask 过大，则进一步改为只用 `zoom_crop_bbox` 或人工 boxes JSON 跑通回投闭环。

## 2026-05-19 VLM 候选选择 Pipeline v2 设计与落地

### 完成内容

- 根据远程 `vlm_pilot_005: Bag / lift_carry / hook` 的后续检查，确认 Florence-2 + SAM2 路线虽然可以跑通，但仍存在系统性定位问题：
  - Florence-2 能生成 `bag handle / handle loop` 的文本响应，但 box 经常覆盖整物体或包体上半部分。
  - SAM2 会被错误大框带偏，分割出大面积包体，而不是 hook 应标注的提手/环状结构。
  - 对稀疏点云渲染图直接做开放词汇 2D 精确定位，不适合作为主线数据构建方案。
- 将主线方案升级为 `VLM-guided Candidate Selection Pipeline v2`：
  - 先从真实 3D 点云和已有弱标签生成通用候选区域；
  - 将候选 A/B/C/D 渲染成多视角 overlay；
  - 让 Qwen3-VL 只做候选编号选择，不再输出 box / point / mask；
  - 再用 gripper / suction / hook / dexterous_hand 的机制规则过滤；
  - 最后合成四通道 candidate mask 并进入网页人工审查。
- 新增通用 v2 脚本，避免继续把逻辑写成 hook 或 handle 特例。

### 产出文件

- `configs/vlm_candidate_pipeline_v2.yaml`
- `tools/generate_3d_candidate_regions.py`
- `tools/render_candidate_overlays_v2.py`
- `tools/run_vlm_candidate_selection_v2.py`
- `tools/filter_candidates_by_executor_rules.py`
- `tools/build_v2_candidate_masks.py`
- `docs/VLM候选选择Pipeline_v2设计与汇报.md`
- 更新 `docs/VLM语义部件引导标注Pipeline.md`
- 更新 `AGENT.md`
- 更新 `.gitignore`
- 更新 `README.md`

### 新判断

VLM 不应作为稀疏点云渲染图上的像素级标注器，而应作为“语义部件和执行器机制选择器”。几何候选生成器负责提供高召回、可解释的候选区域；VLM 负责在候选中判断任务相关性；规则过滤负责执行器机制一致性；人工审查负责最终质量控制。

### 本地检查

- `python -m py_compile` 检查 5 个 v2 新脚本通过。
- 已对 `vlm_pilot_005` 跑通轻量闭环：
  - 生成 18 个通用 3D 候选区域；
  - 生成 8 个视角的候选 overlay；
  - `run_vlm_candidate_selection_v2.py --validate-only` 通过；
  - `--dry-run` 下 VLM 没有真实选择，规则过滤已被修正为只给 `uncertain`，不会自动写入正例；
  - `build_v2_candidate_masks.py --allow-empty` 生成的格式样本通过 `check_dataset.py`。
- 自我纠错：发现初版规则过滤在没有真实 VLM 投票时仍可能仅凭几何规则 accept 多个候选，已修改为“存在 selection 文件但无 VLM 信号时只能进入 uncertain”。

### 下一步

- 先对 `vlm_pilot_005` 运行 v2 单样本闭环，检查候选生成、overlay、VLM selection、rule filter 和 candidate mask 是否能顺利衔接。
- 若单样本可用，再扩展到 8 条 pilot 样本。
- 对网页人工审查结果进行统计，反向调整候选生成器阈值和执行器规则过滤标准。

## 2026-05-19 v2 候选选择失败反馈与语义-候选桥接修正

### 完成内容

- 远程运行 `run_vlm_candidate_selection_v2.py` 后，Qwen3-VL 对 `vlm_pilot_005` 的 8 个视角全部输出空选择。
- 复盘发现这不是简单的 VLM 识别失败，而是 v2 初版仍存在两个桥接问题：
  - 候选列表优先展示 `existing_gripper_weak_mask`、`smooth_surface`、`thin_structure` 等抽象几何名，VLM 看不到这些候选与上游 `bag handle / handle loop` 语义目标之间的关系。
  - 候选生成器缺少通用的“脱离主体/主体上方小结构”候选，导致 handle 类结构没有作为清晰、优先的候选进入 selector。
- 修改 `generate_3d_candidate_regions.py`：
  - 新增基于多视角 `point_index_map` 的通用视觉组件候选；
  - 优先生成 `above_main_body_structure`、`detached_upper_or_side_component`、`small_visual_component`；
  - 对这些候选增加更严格的面积上限，避免把大面积主体作为 target proposal；
  - 将 gripper/hand 弱 mask 说明为“空间先验”，允许其辅助 hook handle 判断，但仍不作为最终真值。
- 修改 `run_vlm_candidate_selection_v2.py`：
  - 读取 `processed/vlm_semantic_part/part_plans/{pilot_id}/combined_part_plan.json`；
  - 在 prompt 中加入上游 Qwen3-VL 语义部件规划结果，如 `bag handle / handle loop`；
  - 明确要求 VLM 不要因为候选名称是 `thin_structure` 或 `existing_gripper_weak_mask` 就直接拒绝，应结合空间位置与目标部件判断。
- 本地重新生成 `vlm_pilot_005` 候选后，候选 A 已变为 `above_main_body_structure`，共 12 个点；候选 B 为 `existing_gripper_weak_mask`，共 18 个点，二者都比原先更接近 hook handle pilot 的审查目标。

### 产出文件

- 更新 `tools/generate_3d_candidate_regions.py`
- 更新 `tools/run_vlm_candidate_selection_v2.py`
- 更新 `tools/render_candidate_overlays_v2.py`

### 本地检查

- `python -m py_compile` 检查相关脚本通过。
- `generate_3d_candidate_regions.py --pilot-id vlm_pilot_005 --overwrite` 通过。
- `render_candidate_overlays_v2.py --pilot-id vlm_pilot_005 --overwrite` 通过。
- `run_vlm_candidate_selection_v2.py --pilot-id vlm_pilot_005 --validate-only` 通过。

### 下一步

- 远程服务器拉取更新后，重新运行：
  - `generate_3d_candidate_regions.py`
  - `render_candidate_overlays_v2.py`
  - `run_vlm_candidate_selection_v2.py`
- 重点检查 `combined_selection.json` 中候选 A/B 是否被选为 `selected_candidates` 或至少进入 `uncertain_candidates`。

## 2026-05-19 v2 规则过滤过宽问题修正

### 完成内容

- 远程 `vlm_pilot_005` 在加入语义部件桥接后，Qwen3-VL 已能稳定选择候选：
  - `A/E/I` 获得 8 个视角投票；
  - `D/G` 获得 6 个视角投票；
  - `H/J` 获得 4 个视角投票。
- 复盘新的 `rule_filter.json` 后发现规则过滤仍然过宽：
  - `L/M/O/P/R` 没有出现在 VLM 的候选列表中，却被纯几何规则接受；
  - `D/G/H/J` 虽然有部分 VLM 投票，但在设置 `--min-selected-votes 8` 时仍被接受；
  - 这会把普通边缘、极值边界或未展示给 VLM 的候选混入 hook 正例。
- 修改 `tools/filter_candidates_by_executor_rules.py`：
  - 新增 `--require-vlm-for-accept / --no-require-vlm-for-accept`，默认启用；
  - 当 VLM selection 已运行且存在投票信号时，候选必须达到 `--min-selected-votes` 才能进入 `accepted_candidates`；
  - 被 VLM 看过但票数不足的候选降为 `uncertain`；
  - 没有展示给 VLM 的候选直接拒绝，不能仅凭规则进入正例。

### 新判断

当前 v2 pipeline 应保持“VLM 负责召回语义候选，规则负责机制审查”的分工。规则过滤器不能在 VLM 已运行的情况下重新引入 VLM 没有看过或没有支持的候选，否则会破坏 candidate mask 的可解释性。

### 本地检查

- `python -m py_compile MultiEEAffordance\tools\filter_candidates_by_executor_rules.py` 通过。

### 下一步

- 远程重新运行 `filter_candidates_by_executor_rules.py --min-selected-votes 8 --overwrite`。
- 预期更保守的结果应主要接受 `A/E/I`，将 `D/G/H/J` 降为 uncertain，并拒绝未展示给 VLM 的 `L/M/O/P/R`。
- 先用 `A` 或 `A,E,I` 生成候选 mask，进入网页人工审查。

## 2026-05-19 v2 候选人工审查可视化增强

### 完成内容

- 远程已用 `build_v2_candidate_masks.py --selected-candidates A` 生成 `vlm_pilot_005` 的保守候选 mask：
  - executor：`hook`
  - selected candidate：`A`
  - positive points：12
- 复盘发现现有 `render_candidate_overlays_v2.py` 的 overlay 更适合 VLM 选择，不适合人工审查：
  - 所有候选叠在一张图上，颜色和点云相互遮挡；
  - 只看 overlay 难以判断 A/B/C... 每个编号具体落在哪里；
  - 已选候选 A 的 12 个点需要单独高亮和局部放大。
- 新增 `tools/visualize_v2_candidates.py`：
  - 为指定 `pilot_id` 和 `selected_candidates` 生成人工审查图；
  - `{view}_selected_context.png`：左侧原始渲染，中间仅显示已选候选，右侧局部放大；
  - `{view}_candidate_grid.png`：每个候选编号单独显示，避免多候选重叠；
  - `index.html`：按视角汇总所有审查图，便于在浏览器或 VSCode 中查看。
- 更新 `docs/VLM候选选择Pipeline_v2设计与汇报.md` 和 `README.md`，加入新脚本说明和运行命令。

### 本地检查

- `python -m py_compile MultiEEAffordance\tools\visualize_v2_candidates.py` 通过。

### 下一步

- 远程拉取后运行：
  - `visualize_v2_candidates.py --pilot-id vlm_pilot_005 --selected-candidates A --overwrite`
- 打开 `processed/vlm_candidate_v2/review_visualizations/vlm_pilot_005/index.html`，检查候选 A 是否确实对应 bag handle / handle loop。
- 如果 A 太少或只覆盖 handle 的一小段，再人工对比 `candidate_grid` 中的 E/I，决定是否用 `A,E,I` 生成第二版候选 mask。

## 2026-05-19 v2 模块化 Pipeline 与点级人工审查系统初版

### 完成内容

- 针对“候选生成、VLM 投票、规则过滤、候选 mask 构建、人工审查”分散运行的问题，新增模块化集成脚本：
  - `tools/run_v2_pipeline.py`
  - 支持按阶段运行：`generate,render,select,filter,build,visualize`
  - 每次运行写出 `processed/vlm_candidate_v2/pipeline_runs/latest_run_manifest.json`，记录命令和返回码。
- 针对“协作审查需要点级编辑”的需求，新增点级审查网页 MVP：
  - `tools/serve_v2_annotation_app.py`
  - 支持加载 `v2_candidate_samples_v0_1.jsonl`；
  - 支持在网页中旋转/缩放点云；
  - 支持对目标执行器通道进行单点添加、删除和切换；
  - 保存后输出 manual refined mask、refined samples JSONL 和 append-only 审查日志。
- 新增设计文档：
  - `docs/v2模块化Pipeline与点级审查系统设计.md`
  - 说明模块化 pipeline、网页审查 MVP、输出文件、多人协作部署建议和当前限制。
- 更新：
  - `README.md`
  - `.gitignore`

### 新判断

当前阶段的多人审查系统应先作为实验室内网/服务器 MVP 使用，不建议直接公网裸奔。正式上线前需要至少增加登录、任务分配、样本锁定、审查日志备份和 HTTPS 反向代理。

### 本地检查

- `python -m py_compile MultiEEAffordance\tools\run_v2_pipeline.py` 通过。
- `python -m py_compile MultiEEAffordance\tools\serve_v2_annotation_app.py` 通过。

### 下一步

- 远程服务器拉取更新后，先用单样本启动点级审查系统：
  - `serve_v2_annotation_app.py --samples processed/metadata/v2_candidate_samples_v0_1.jsonl --host 0.0.0.0 --port 8770`
- 在网页中对 `vlm_pilot_005` 的候选 A 删除 false positive 点，保存 refined mask。
- 如果单点编辑效率不足，再增加框选/套索删除功能。

## 2026-05-20 v2 审查系统升级为候选勾选 + 点级精修

### 完成内容

- 针对“批量处理时不能每条样本都手动指定 A”的问题，升级 `tools/serve_v2_annotation_app.py`：
  - 后端读取每条样本的 `candidate_manifest`、`candidate_masks.npz` 和 `rule_filter.json`；
  - 根据当前样本的 `selected_candidates`、规则 accepted/uncertain、VLM selected votes 和 rule score 生成 top-k 候选菜单；
  - 前端显示候选 ID、候选名称、候选类型、VLM 投票、规则分数和状态；
  - 审查者可以勾选多个候选，例如 `A`、`A+E`、`A+E+I`；
  - 点击“应用勾选候选”后，系统将候选组合合并成当前 mask；
  - 随后继续支持点级添加、删除和切换；
  - 保存时在审查记录中写入 `selected_candidate_ids`，方便追踪人工最终采用了哪些候选。
- 新增启动参数：
  - `--top-k-candidates`：控制审查页面显示多少个排序候选，`0` 表示显示全部。
- 更新 `docs/v2模块化Pipeline与点级审查系统设计.md`，说明候选菜单式人工审查流程。

### 新判断

批量流程中不应固定采用候选 A。系统应提供“自动排序后的候选菜单”，由人工选择候选组合并进行点级 refinement。这样既利用 VLM/规则提高召回，又保留人工对最终 mask 的控制权。

### 本地检查

- `python -m py_compile MultiEEAffordance\tools\serve_v2_annotation_app.py` 通过。
- `serve_v2_annotation_app.py --help` 显示新增 `--top-k-candidates` 参数。

### 下一步

- 远程拉取后重新启动审查系统：
  - `serve_v2_annotation_app.py --top-k-candidates 8`
- 在 `vlm_pilot_005` 上测试：
  - 默认从当前候选 mask 加载；
  - 勾选 `A+E` 或 `A+E+I`；
  - 点击“应用勾选候选”；
  - 删除无关点并保存 refined mask。
- 如果审查者反馈单点删除太慢，再增加框选删除和套索选择。

## 2026-05-20 v2 候选菜单与点云实时联动预览

### 完成内容

- 针对“候选列表只能整体叠加，分不清每个候选区域位置”的问题，升级 `tools/serve_v2_annotation_app.py` 前端交互：
  - 鼠标悬停某个候选卡片时，点云中实时单独高亮该候选；
  - 点击某个候选卡片时，保持该候选为当前预览焦点；
  - 新增“预览勾选组合”按钮，只显示当前勾选候选的组合区域；
  - 新增“取消候选预览”按钮，回到全局候选预览；
  - 点云 HUD 中显示当前 preview 的候选 ID，方便确认正在看哪个候选。
- 更新 `docs/v2模块化Pipeline与点级审查系统设计.md`，补充候选实时联动预览说明。

### 本地检查

- `python -m py_compile MultiEEAffordance\tools\serve_v2_annotation_app.py` 通过。

### 下一步

- 远程重新拉取后重启审查服务。
- 在 `vlm_pilot_005` 中分别悬停/点击 `A`、`E`、`I`，确认点云图能实时高亮对应区域。

## 2026-05-20 v2 候选勾选与锁定预览交互修正

### 完成内容

- 根据实际网页测试反馈，修正 `tools/serve_v2_annotation_app.py` 中候选列表的交互问题：
  - 原先悬停候选卡片时会重建候选列表 DOM，导致 checkbox 勾选容易失效；
  - 现在悬停只做临时预览，不重建列表；
  - 点击候选卡片会锁定该候选预览，候选卡片显示锁定态；
  - checkbox 点击只负责勾选/取消候选组合，不再触发候选锁定；
  - “预览勾选组合”会锁定当前 checkbox 勾选的候选集合；
  - “取消候选预览”会解除锁定并回到普通显示。
- 移除当前审查页面中的 `reviewer` 输入框，减少人工审查时的表单负担。
- 更新 `docs/v2模块化Pipeline与点级审查系统设计.md`，明确“悬停预览、点击锁定、勾选组合、应用成 mask”的交互区别。

### 本地检查

- `python -m py_compile MultiEEAffordance\tools\serve_v2_annotation_app.py` 通过。

### 下一步

- 远程拉取后重启审查服务，并强制刷新浏览器页面。
- 在候选列表中测试：
  - 悬停 `A/E/I` 能临时显示对应候选；
  - 点击 `A` 后即使鼠标移开也能锁定显示；
  - checkbox 能正常勾选多个候选；
  - 点击“应用勾选候选”后，点云中的当前 mask 变为勾选候选的并集。

## 2026-05-19 点云转 Mesh 增强 VLM 识别独立实验区

### 完成内容

- 针对“点云过于稀疏，VLM 难以识别结构”的新想法，新增独立实验区：
  - `experiments/mesh_surface_vlm/`
- 明确实验边界：
  - 不改动现有 v2 pipeline；
  - mesh 只作为 VLM 视觉辅助；
  - 最终 affordance mask 仍必须落回原始点云 `[N,4]`；
  - mesh 不能直接作为 ground truth，因为可能填洞、补假面或改变 hook 所需的开放结构。
- 新增 `experiments/mesh_surface_vlm/README.md`：
  - 记录实验目的、原则、运行命令、评估标准；
  - 对 Poisson、Ball Pivoting、Alpha Shape 的优缺点做了初步分析。
- 新增 `experiments/mesh_surface_vlm/reconstruct_pointcloud_mesh.py`：
  - 支持 `--pilot-id` 自动从 metadata 找点云；
  - 支持 `poisson`、`ball_pivoting`、`alpha_shape` 和 `all`；
  - 输出 mesh 文件和 `mesh_reconstruction_summary.json`。
- 新增 `experiments/mesh_surface_vlm/render_mesh_views.py`：
  - 对 mesh 生成多视角 VLM-friendly PNG；
  - 输出 `mesh_render_manifest.json`。
- 更新 `.gitignore`：
  - 忽略 `processed/mesh_surface_vlm/` 实验输出。

### 初步判断

点云转 mesh 是值得验证的视觉增强方向，但存在明显风险：

- 对 suction / gripper 的表面理解可能有帮助；
- 对 hook 任务要非常谨慎，因为 mesh reconstruction 可能把孔洞、环、把手内侧填平；
- 更稳妥的对照实验是同时比较：
  - 原始 dense point render；
  - mesh render；
  - silhouette / surface splatting render。

### 本地检查

- `python -m py_compile MultiEEAffordance\experiments\mesh_surface_vlm\reconstruct_pointcloud_mesh.py MultiEEAffordance\experiments\mesh_surface_vlm\render_mesh_views.py` 通过。

### 下一步

- 服务器端优先用 `vlm_pilot_005` 跑 mesh reconstruction。
- 比较 Poisson、Ball Pivoting、Alpha Shape 三种 render 中 bag handle 是否更清晰。
- 如果 mesh render 能让 VLM 更稳定识别 handle，后续再考虑作为 v2 的可选视觉输入，而不是替换 v2。

## 2026-05-20 Mesh 实验的 Component-aware 与 Hybrid Render 修正

### 完成内容

- 根据纯 mesh render 的失败现象进行修正：
  - 把手结构在重建后消失；
  - 主体表面出现洞和不稳定三角面；
  - 说明纯“稀疏点云 -> mesh”不适合直接承担 hook/handle 类结构表达。
- 修改 `experiments/mesh_surface_vlm/reconstruct_pointcloud_mesh.py`：
  - 新增 `--preserve-candidates`；
  - 支持将候选 `A,E` 等从 mesh 重建输入中排除；
  - 输出 `preserved_candidate_points.npy`、`preserved_candidate_mask.npy`、`body_points_for_mesh.npy`；
  - 在启用 preserve 后输出 `poisson_body_mesh.ply`、`ball_pivoting_body_mesh.ply`、`alpha_shape_body_mesh.ply`。
- 修改 `experiments/mesh_surface_vlm/render_mesh_views.py`：
  - 新增 `--pilot-id` / `--points` 自动加载原始点云；
  - 新增 `--overlay-points`，将原始点云叠加到 mesh render；
  - 新增 `--overlay-candidates A,E`，高亮指定候选点；
  - 新增 `--mesh-alpha`、`--point-alpha`、`--point-size`、`--candidate-point-size`，方便调节可视化。
- 更新 `experiments/mesh_surface_vlm/README.md`：
  - 增加 component-aware mesh 和 hybrid render 的运行命令。

### 新判断

后续 mesh 实验不再追求“用 mesh 替代点云”，而是验证：

```text
主体 mesh 作为连续背景
+ 原始点云作为真实几何依据
+ A/E 等候选点作为功能部件高亮
```

这个 hybrid render 更符合当前数据构建目标：帮助 VLM/人工看清结构，但最终标签仍回到原始点云。

### 本地检查

- `python -m py_compile MultiEEAffordance\experiments\mesh_surface_vlm\reconstruct_pointcloud_mesh.py MultiEEAffordance\experiments\mesh_surface_vlm\render_mesh_views.py` 通过。

### 下一步

- 服务器端用 `--preserve-candidates A,E` 重建 body mesh。
- 用 `--overlay-candidates A,E` 渲染 hybrid views。
- 对比纯 mesh、dense point render、hybrid render 三者，判断 hybrid 是否能保留 handle 并减少 VLM 误判。

## 2026-05-20 v2 候选勾选集合实时显示修正

### 完成内容

- 根据审查页面实际使用反馈，进一步修正 `tools/serve_v2_annotation_app.py` 的候选可视化逻辑：
  - 页面加载样本后，默认显示当前已勾选候选的集合，而不是显示全部候选叠加；
  - 每次勾选或取消勾选 checkbox，点云图立即切换为当前勾选候选的并集；
  - 点击候选卡片仍保留为“单独查看并锁定某个候选”的功能；
  - checkbox 的语义明确为“准备应用到 mask 的候选组合”；
  - 页面说明文字同步改为“勾选框表示当前准备采用的候选组合，点云会实时显示已勾选候选的集合”。
- 更新 `docs/v2模块化Pipeline与点级审查系统设计.md`，明确勾选集合会实时显示。

### 本地检查

- `python -m py_compile MultiEEAffordance\tools\serve_v2_annotation_app.py` 通过。

### 下一步

- 远程重启审查服务后强制刷新浏览器。
- 测试勾选 `A`、`A+E`、`A+E+I` 时，点云图是否分别显示对应集合。
- 确认集合无误后，再点击“应用勾选候选”进入点级精修。

## 2026-05-20 v2 点级删除与候选预览冲突修正

### 完成内容

- 根据“当前无法删除点”的实际反馈，修正 `tools/serve_v2_annotation_app.py` 的编辑状态逻辑：
  - 原问题是 checkbox 勾选候选后，点云显示的是候选预览集合，但真实可编辑的 `positives` mask 没有同步更新；
  - 删除模式只删除 `positives` 中的点，因此点击候选预览点时会表现为删不掉；
  - 同时，候选预览颜色可能在点被删除后继续把该点画出来，造成“视觉上没有删除”的错觉。
- 当前修正为：
  - checkbox 勾选/取消时，立即把当前勾选候选的并集同步为可编辑 mask；
  - “应用勾选候选”保留，但本质上是重新应用当前勾选集合；
  - 删除模式下，非正样本点不再显示候选预览颜色，保证删掉的点能立刻从当前 mask 视觉中消失；
  - 页面加载时不再自动用候选预览覆盖当前 mask，避免 refined mask 被候选预览干扰。

### 本地检查

- `python -m py_compile MultiEEAffordance\tools\serve_v2_annotation_app.py` 通过。

### 下一步

- 远程重启审查服务并强制刷新页面。
- 勾选 `A+E` 后进入“只删除”模式，点击候选中的误标点，确认点能从当前 mask 中消失。
- 保存 refined mask 后重新加载样本，确认删除结果仍然保留。

## 2026-05-20 Pilot 人工审查扩展到 10 条

### 完成内容

- 在 `processed/metadata/vlm_pilot_samples_v0_1.csv` 中将 pilot 样本从 8 条扩展到 10 条。
- 新增两条 pilot：
  - `vlm_pilot_009`：`Faucet / open_pull / dexterous_hand`，用于检查灵巧手对水龙头把手/旋钮类精细操作区域的标注，避免泛化到普通管体；
  - `vlm_pilot_010`：`Scissors / pick_up / hook`，用于检查 hook 对剪刀指环/孔洞边界可挂接区域的漏标补充。
- 本地检查 10 条 pilot 的 `point_cloud_path`、`source_mask_path`、`checked_mask_path` 均存在。
- 新增 `docs/10条Pilot人工审查工作清单.md`：
  - 记录 10 条 pilot 的审查目的；
  - 给出远程服务器运行 v2 pipeline 的命令；
  - 给出人工审查顺序建议；
  - 给出每条样本的审查动作和需要记录的问题类型。
- 更新 `docs/VLM试验运行手册.md`，将 pilot 数量说明从 8 条改为 10 条。

### 新判断

`vlm_pilot_005` 已经完成首条人工审查，下一阶段应验证流程能否从单样本扩展到小批量 10 条。新增样本优先选择与 `vlm_pilot_005` 不同的结构和执行器机制：Faucet 用于测试 dexterous hand 的精细操作区域，Scissors 用于测试 hook 对环/孔洞结构的可挂接判断。

### 本地检查

- `vlm_pilot_samples_v0_1.csv` 读取成功，共 10 条。
- 10 条 pilot 的点云、原始 mask、checked mask 路径均存在。

### 下一步

- 远程服务器 `git pull` 后运行 10 条 v2 自动候选流程：
  - `run_v2_pipeline.py --limit 10 --stages generate,render,select,filter,build --allow-empty --overwrite`
- 启动 `serve_v2_annotation_app.py`，在网页中按 10 条清单进行人工审查。
- 审查后统计每类失败模式：候选覆盖不足、候选过宽、VLM 选择错误、规则过滤过严/过宽、点级编辑效率问题。

## 2026-05-21 v2 pipeline 补充视图渲染前置阶段

### 完成内容

- 根据远程服务器扩展到 10 条 pilot 时的运行反馈，修正 `tools/run_v2_pipeline.py` 的阶段编排。
- 新增 `views` 阶段，用于调用 `render_vlm_friendly_views.py` 生成：
  - VLM-friendly 多视角 dense render；
  - point-index map；
  - `processed/vlm_semantic_part/renders/{sample_id}/view_manifest.json`。
- 当用户命令中包含 `render` 阶段时，总控脚本会自动先执行 `views`，再执行 `render_candidate_overlays_v2.py`。
- 保持原有远程命令兼容，`--stages generate,render,select,filter,build` 不需要改动。
- 更新 `docs/10条Pilot人工审查工作清单.md`，说明 `render` 阶段现在包含视图渲染前置步骤。
- 在 `docs/报错问题记录.md` 中单独记录本次 `view_manifest.json` 缺失问题、原因和修复方式。

### 新判断

10 条 pilot 小批量流程中，新加入样本不仅需要 3D 候选区域，还必须先生成多视角渲染清单，否则后续 overlay、VLM 选择和网页审查都无法对齐到具体视角。该前置依赖应由总控脚本自动处理，不能要求人工每次额外记住。

### 本地检查

- `python -m py_compile MultiEEAffordance\tools\run_v2_pipeline.py` 通过。
- `run_v2_pipeline.py --dry-run --limit 10 --stages generate,render,select,filter,build --allow-empty --overwrite` 通过。
- dry-run 命令序列已确认自动出现 `views` 阶段，顺序为 `generate -> views -> render -> select -> filter -> build`。

### 下一步

- 本地验证通过后，远程服务器拉取更新并重跑原 10 条命令。
- 如果远程暂时不能拉取新代码，可先手动运行 `render_vlm_friendly_views.py --limit 10 --overwrite` 补齐 view manifest，再重跑原 pipeline。

## 2026-05-21 v2.1 高召回候选生成器升级

### 完成内容

- 根据 10 条 pilot 小规模审查中的反馈，确认旧版候选生成器存在明显问题：
  - 候选区域过于稀疏，更像 seed 点而不是可审查 region；
  - 剪刀等双环/双把手结构容易只覆盖单侧或局部；
  - VLM 只能在已有候选中投票，如果目标部件没有被候选生成器覆盖，VLM 无法凭空补出候选；
  - 过早 `max-candidates` 截断会使合理候选无法进入 VLM 和人工审查。
- 升级 `tools/generate_3d_candidate_regions.py`：
  - 默认 `max_candidates` 从 18 提高到 36；
  - 新增 kNN seed 扩张，将稀疏点扩成可审查候选区域；
  - 新增高曲率/线性点的连通部件候选；
  - 新增 `expanded_loop_or_handle`、`loop_or_hole_boundary`、`paired_loop_or_handle` 等候选族；
  - 新增空间半区/极值区扩张候选，用于双环、双把手、左右对称结构；
  - 新增候选 `priority` 字段，使更适合人工审查的部件级候选排在前面。
- 更新 `tools/filter_candidates_by_executor_rules.py`：
  - 让 hook、gripper、dexterous_hand 规则认识新的环/把手/扩张候选族；
  - 对 `paired_loop_or_handle` 给 hook 额外加分。
- 更新 `tools/render_candidate_overlays_v2.py`：
  - VLM overlay 默认显示候选数从 10 提高到 14；
  - 扩展颜色表，减少候选颜色重复。
- 更新 `tools/run_vlm_candidate_selection_v2.py`：
  - 在提示词中说明 expanded/paired candidates 是为了人工审查而生成的更完整候选；
  - 要求 VLM 如果发现明显目标部件没有候选覆盖，需要在 reason 中说明缺失。
- 更新 `docs/v2模块化Pipeline与点级审查系统设计.md`，记录 v2.1 候选生成器设计。
- 更新 `.gitignore`，忽略本地候选生成验证临时目录 `_tmp_candidate_test/`。

### 新判断

候选生成器的第一目标应是“高召回、可人工审查”，不是直接产出干净正例。自动阶段应该给出完整候选区域和多种候选尺度，让人工在网页中勾选组合并点级精修。对剪刀指环、袋子把手、杯把、水龙头旋钮这类结构，稀疏高曲率点不足以支撑标注，必须引入部件级扩张和成对结构候选。

### 本地检查

- `python -m py_compile MultiEEAffordance\tools\generate_3d_candidate_regions.py MultiEEAffordance\tools\filter_candidates_by_executor_rules.py MultiEEAffordance\tools\render_candidate_overlays_v2.py MultiEEAffordance\tools\run_vlm_candidate_selection_v2.py` 通过。
- 使用 `vlm_pilot_010 / Scissors / hook` 进行临时输出验证，候选数从旧版约 17 条提升到 25 条，并新增 `expanded_loop_or_handle`、`expanded_extreme_part_component` 等区域候选。

### 下一步

- 远程服务器拉取更新后，重新运行 10 条 pilot 的 v2 pipeline。
- 在网页审查系统中优先检查 `vlm_pilot_010`，确认剪刀另一侧指环/把手是否进入 top-k 候选。
- 如果候选仍覆盖不足，下一步再增加 VLM coverage check：让 VLM 明确判断“目标部件是否被候选覆盖”，并触发 missing-candidate 补候选。

---

## 2026-05-21 v2.2 新增 VLM Coverage Check 与 Missing-Candidate 补候选

### 完成内容

- 新增 `tools/run_vlm_coverage_check_v2.py`：
  - 输入 clean render、candidate overlay、part plan、任务和末端执行器定义；
  - 要求 Qwen3-VL 显式判断目标功能部件是否被当前候选覆盖；
  - 输出 `combined_coverage_check.json`，记录 `covered / partially_covered / not_covered / uncertain`；
  - 当 VLM 判断“目标部件可见但未被候选覆盖”时，输出缺失部件的粗 2D box 和 positive points；
  - 通过 point-index map 将缺失区域回投为新的 3D candidate；
  - 新补候选使用 `M1/M2...` 编号，候选族为 `vlm_coverage_missing_region`。
- 更新 `tools/run_v2_pipeline.py`：
  - 新增 `coverage` 阶段；
  - 默认流程扩展为 `generate -> views -> render -> coverage -> render_after_coverage -> select -> filter -> build -> visualize`；
  - 如果 coverage 阶段补出了新候选，会自动再次运行 `render_candidate_overlays_v2.py`，保证新候选能进入后续 VLM selection。
- 更新 `tools/filter_candidates_by_executor_rules.py`：
  - 对 `vlm_coverage_missing_region` 增加规则分数，使其不会被当成普通几何噪声直接压低；
  - 仍然要求后续 VLM selection、规则过滤和人工审查确认。
- 更新 `tools/run_vlm_candidate_selection_v2.py`：
  - 在提示词中说明 `M1/M2` 和 `vlm_coverage_missing_region` 是 coverage check 补出的候选；
  - 要求 VLM 将其作为普通候选审查，不能因为来源特殊就自动通过。
- 更新 `docs/v2模块化Pipeline与点级审查系统设计.md`，加入 v2.2 coverage check 流程说明和推荐运行命令。

### 新判断

当前 pipeline 不应只问“这些候选里哪个最好”，还必须问“目标部件有没有被候选池覆盖”。这是一个关键的自我纠错环节：VLM 的强项是语义和功能部件判断，因此更适合用来发现候选池漏洞；几何和 point-index map 的强项是把 VLM 指出的粗区域转回 3D 点云候选。最终仍不直接信任 VLM，而是把补出的 `M1/M2` 交给后续投票、规则和人工审查。

### 本地检查

- `python -m py_compile MultiEEAffordance\tools\run_vlm_coverage_check_v2.py MultiEEAffordance\tools\run_v2_pipeline.py MultiEEAffordance\tools\filter_candidates_by_executor_rules.py MultiEEAffordance\tools\run_vlm_candidate_selection_v2.py` 通过。
- `run_v2_pipeline.py --dry-run` 已确认阶段顺序会自动插入 `render_after_coverage`。
- `run_vlm_coverage_check_v2.py --dry-run --no-supplement-missing` 已确认能读取现有 `vlm_pilot_005` overlay 并写出 coverage check 结构。

### 下一步

- 远程服务器拉取代码后，优先对 10 条 pilot 运行带 coverage 的 v2.2 pipeline。
- 检查 `processed/vlm_candidate_v2/coverage_check/<pilot_id>/combined_coverage_check.json` 中是否出现 `not_covered` 或 `partially_covered`。
- 如果出现 `added_candidates: ["M1", ...]`，打开新 overlay 和网页审查系统，确认 `M1/M2` 是否真正覆盖缺失目标部件。

---

## 2026-05-21 v3 语义目标-拒绝候选 Pipeline 初版完成

### 完成内容

- 根据 10 条 pilot 扩展实验中的失败反馈，确认 v2/v2.2 的根本问题是“几何候选优先”，容易把剪刀刃、普通长边界、主体边缘等几何上显著但机制上错误的区域送入候选池。
- 新增 v3 pipeline，核心思想改为“语义 target / reject 先行”：
  - `tools/run_v3_semantic_part_planner.py`：用 Qwen3-VL 生成 target_positive_parts、target_grounding_queries、reject_negative_parts、reject_grounding_queries；
  - `tools/run_v3_target_reject_grounding.py`：对 target/reject 部件输出粗 2D box 与 positive points；
  - `tools/project_v3_grounding_to_3d.py`：利用 point-index map 将 2D target/reject 回投为 3D votes；
  - `tools/grow_v3_reject_aware_candidates.py`：只从 target seed 生长候选，并用 reject veto 硬排除 blade/cutting edge/tip 等错误区域；
  - `tools/build_v3_candidate_masks.py`：生成可进入网页审查系统的 `[N,4]` 候选 mask；
  - `tools/run_v3_pipeline.py`：将 views、plan、ground、project、grow、render、build、visualize 阶段串成一条远程可复现命令。
- v3 对 `Scissors / hook` 增加明确先验：target 为 `finger holes / handle loops / inner rim`，reject 为 `blade / cutting edge / blade tip / ordinary blade boundary`。
- 复用并小幅改进 `tools/visualize_v2_candidates.py`，使其在 v3 下可以读取 `candidate_manifest.json` 中的 `default_selected_candidates`，不用每次手动指定候选 id。
- 新增设计与汇报文档：`docs/V3语义目标拒绝候选Pipeline设计与汇报.md`，说明 v2 失败原因、v3 模块划分、运行命令和人工审查重点。

### 新判断

v3 不再追求“自动直接产出 ground truth”，而是把自动阶段定位为高质量候选生成器。VLM 的强项用于判断目标语义和机制负例，几何与 point-index map 的强项用于把这些判断约束回真实 3D 点云。最终仍由网页人工审查完成候选组合选择和点级精修。

### 本地检查

- `python -m py_compile MultiEEAffordance\tools\run_v3_semantic_part_planner.py MultiEEAffordance\tools\run_v3_target_reject_grounding.py MultiEEAffordance\tools\project_v3_grounding_to_3d.py MultiEEAffordance\tools\grow_v3_reject_aware_candidates.py MultiEEAffordance\tools\build_v3_candidate_masks.py MultiEEAffordance\tools\run_v3_pipeline.py MultiEEAffordance\tools\visualize_v2_candidates.py` 通过。
- `run_v3_pipeline.py --dry-run --pilot-id vlm_pilot_010 --stages views,plan,ground,project,grow,render,build,visualize --allow-empty --overwrite` 通过，命令顺序和 v3 输出目录确认正确。

### 下一步

- 远程服务器拉取本地代码后，优先对 `vlm_pilot_010: Scissors / pick_up / hook` 跑 v3 pipeline，检查 semantic plan 是否正确把剪刀指环作为 target，并把剪刀刃/刀尖作为 reject。
- 若 `target_reject_grounding` 的 2D 粗定位仍偏大，优先尝试 `--box-shrink-ratio 0.05` 或 `0.10`；若候选过稀疏，尝试 `--expand-hops 2`。
- 生成 `v3_candidate_samples_v0_1.jsonl` 后，用现有网页审查系统加载 v3 样本，检查候选组合勾选和点级增删是否足够支撑小规模人工审查。

---

## 2026-05-25 点级审查系统交互性能优化

### 完成内容

- 针对网页审查系统在切换物体点云时延迟高、偶发无响应的问题，优化 `tools/serve_v2_annotation_app.py`。
- 服务端增加 sample payload 缓存，避免反复从磁盘读取同一条样本的点云、mask 和候选 npz；保存 refined mask 后只失效当前样本缓存。
- 前端增加样本缓存、加载序号和请求取消逻辑：
  - 已加载过的样本再次切换时直接从浏览器缓存恢复；
  - 连续点击不同样本时取消上一条 `/api/sample` 请求；
  - 即使旧请求晚返回，也会被加载序号拦截，避免旧样本覆盖新样本。
- 前端画布渲染改为 `requestAnimationFrame` 合并绘制，避免拖动旋转、候选 hover、滚轮缩放时同步触发过多重绘。
- 增加点云投影缓存和候选颜色映射缓存，避免每次绘制都重新投影、排序全部点并逐点遍历候选集合。
- 保存 refined mask 后不再先自动加载第一条样本，再加载当前样本；现在只刷新样本列表并强制重载当前样本。

### 新判断

审查系统进入 10 条 pilot 和后续批量审查后，交互延迟本身会直接影响人工标注效率。当前前端瓶颈不在点云数量本身，而在重复计算和请求竞态：同一批点云在旋转、候选预览和样本切换时被反复投影、排序、遍历候选集合，并且旧请求可能晚到覆盖新状态。该问题应在网页层通过缓存、请求取消、绘制合并解决，而不是要求人工减少候选或减少样本。

### 本地检查

- `python -m py_compile MultiEEAffordance\tools\serve_v2_annotation_app.py` 通过。

### 下一步

- 远程服务器拉取代码后重启审查网页服务，并在浏览器中强制刷新页面。
- 重点测试：连续快速点击左侧 10 条样本、候选 hover/锁定、勾选组合、删除点、保存后重载当前样本。

---

## 2026-05-25 点级审查系统唯一样本键与候选降噪

### 完成内容

- 修复同一个 `sample_id` 对应多个 pilot/executor 时的审查系统混淆问题。
  - 新增内部唯一键 `row_key = pilot_id|sample_id|executor`。
  - 左侧列表 active 高亮、样本加载、浏览器缓存、保存 refined mask 均改为使用 `row_key`。
  - 后端 refined 结果也按 `row_key` 管理，避免同一物体不同执行器任务互相覆盖。
- 修复 pending 样本点击后不显示点云的问题。
  - 候选上下文读取失败时不再让整个 `/api/sample` 失败；
  - mask 加载增加 `multi_channel_mask_path -> checked_mask_path -> source_mask_path` 回退；
  - 单通道 mask 会自动放入当前 executor 通道用于显示。
- 候选列表默认降噪。
- `--top-k-candidates` 默认从 8 降为 5；
- 新增 `--candidate-min-selected-votes`，默认只展示 VLM selected votes 大于等于 2 的非默认候选；
  - 低置信 rule-only 候选默认不再塞给人工审查，减少人工选择负担。
- 页面候选说明中增加 `shown_candidates/total_candidates` 和候选 warning，方便判断当前是不是“没有高置信候选”而不是系统坏了。

### 新判断

人工审查界面必须以“审查任务”为单位，而不是以“物体 sample_id”为单位。同一个物体可能需要针对不同 executor 或不同 pilot 进行独立审查，若以 `sample_id` 作为唯一键，会造成高亮、缓存、保存和 refined 结果全部混淆。候选选择也应优先呈现高置信候选，让人工在少量可靠候选中抉择；低置信候选需要通过参数显式打开，而不是默认增加审查负担。

### 本地检查

- `python -m py_compile MultiEEAffordance\tools\serve_v2_annotation_app.py` 通过。
- 抽取页面内 JS 并执行 `node --check` 通过。

### 下一步

- 远程重启审查服务并强制刷新网页，确认两个 Earphone 样本不再同时高亮。
- 逐个点击 pending 样本，若候选文件仍有问题，页面应显示点云主体和候选 warning，而不是空白无响应。
- 若确实需要临时查看低置信候选，可启动服务时传入 `--candidate-min-selected-votes 0`。

---

## 2026-05-25 点级审查系统圆形刷子编辑

### 完成内容

- 将点级增删从“单点点击”升级为“圆形刷子编辑”。
- 在工具栏新增 brush 半径滑条，默认半径 24px，可在 6px 到 70px 之间调整。
- 在 `add/delete/toggle` 模式下，画布会显示当前 brush 圆圈：
  - `add`：圆圈范围内点加入当前正例 mask；
  - `delete`：圆圈范围内正例点从当前 mask 删除；
  - `toggle`：按下时根据圆圈内多数状态决定本次笔画是 add 还是 delete，拖动过程中保持同一个动作，避免来回扫过时反复翻转。
- 交互方式调整为左键进行 brush 增删，右键拖动旋转点云，滚轮缩放；画布右键菜单已禁用，人工审查时不需要频繁切回查看模式。
- 鼠标按下并拖动可以连续刷选/刷删，一次操作只记录一个 undo 历史状态，便于撤销。

### 新判断

人工审查阶段不应要求逐点点击，尤其是候选区域存在少量 false positive 或需要补一小片连续区域时，逐点编辑效率过低。圆形 brush 更符合点云区域修正的实际动作：自动候选负责给出大致范围，人工通过小半径刷子完成局部清理和补点。

### 本地检查

- `python -m py_compile MultiEEAffordance\tools\serve_v2_annotation_app.py` 通过。
- 抽取页面内 JS 并执行 `node --check` 通过。

### 下一步

- 远程重启服务并强制刷新页面，测试 brush 半径、拖动删除、拖动添加、撤销是否符合预期。

---

## 2026-05-25 自然化点云表面渲染与回投实验模块

### 完成内容

- 针对“稀疏点云图不够像自然图像，VLM 难以稳定识别任务部件”的问题，新增独立实验目录 `experiments/natural_surface_vlm/`，不改动现有 v2/v3 主流程。
- 新增 `render_natural_surface_views.py`：
  - 使用 surface splatting 将稀疏点云渲染为更连续、更接近自然表面的多视角图；
  - 同步保存 `point_index.npy`、`exact_point_index.npy`、`confidence.npy`、`source.npy`；
  - `source` 区分背景、直接点 splat、邻近填补像素，避免自然化视觉图失去回投依据；
  - 输出 `panel.png`，同时展示自然化渲染、回投置信度和前景放大，便于人工判断图像是否真的更适合 VLM。
- 新增 `project_natural_masks_to_3d.py`：
  - 输入 VLM/SAM2 在自然化渲染图上得到的 2D mask；
  - 通过 `point_index/confidence/source` 回投到原始点云；
  - 输出单通道点 mask、点级 view votes、pixel votes，以及指定 executor 通道的 `[N,4]` candidate mask。
- 新增中文说明文档 `experiments/natural_surface_vlm/README.md`，说明该实验的目标、运行命令、输出字段、参数调节和人工检查标准。
- 更新 `.gitignore`，忽略 `processed/natural_surface_vlm/` 下的实验输出。

### 新判断

当前阶段不应把“点云转 mesh”作为主路径，因为 mesh 重建可能吞掉 handle、ring、hole 等 affordance 关键细结构。更稳妥的方向是把自然化渲染作为 VLM 视觉代理，同时严格维护每个有效像素到原始点云索引的映射。这样 VLM 可以看更接近物体表面的图，但最终 3D candidate mask 仍然落回原始点云，不把补出来的连续表面当成几何真值。

### 本地检查

- `python -m py_compile MultiEEAffordance\experiments\natural_surface_vlm\render_natural_surface_views.py MultiEEAffordance\experiments\natural_surface_vlm\project_natural_masks_to_3d.py` 通过。

### 下一步

- 在服务器上优先对 `vlm_pilot_005` 生成自然化多视角图，重点查看 `yaw000/yaw045/yaw180` 的 `panel.png`。
- 若自然化图能让 VLM 更稳定识别 `bag handle`，下一步将 Qwen3-VL/SAM2 的 2D mask 输出接入 `project_natural_masks_to_3d.py`，生成可进入网页人工审查的 3D candidate mask。
- 对比三类视觉输入：原始 dense point render、mesh/hybrid render、natural surface render，判断哪一种最适合作为后续 VLM 标注辅助输入。

---

## 2026-05-25 自然化渲染视觉质量第一轮优化

### 完成内容

- 根据 `vlm_pilot_005` 的渲染结果反馈，确认初版自然化渲染存在两个主要问题：
  - 点云被膨胀成明显的圆点/球状颗粒，仍然不像自然图像；
  - 内部 hole/fill 边界被画成大量黑色裂纹，会干扰 VLM 对 handle、body 等部件的理解。
- 更新 `experiments/natural_surface_vlm/render_natural_surface_views.py`：
  - 新增 `--edge-mode none/silhouette/depth`，默认使用 `silhouette`，只强调外轮廓，避免内部裂纹铺满物体表面；
  - 新增 `--blur-radius`，默认使用高斯模糊弱化圆点颗粒感；
  - 将默认 `fill-radius` 从 5 提高到 10，让小孔洞更容易被视觉代理填平；
  - 默认禁止向外部背景填补，只填内部小孔，减少边缘和 handle 被渲染成“胖球串”的问题；
  - 新增 `--fill-external-background` 作为诊断/强填补选项，默认不建议给 VLM 图使用；
  - 保留 `point_index/confidence/source` 回投机制不变，视觉质量优化不改变“最终 mask 必须回投到原始点云”的原则。
- 更新 `experiments/natural_surface_vlm/README.md`，增加推荐参数和调参方向：
  - 更连续：`--splat-radius 12 --fill-radius 14 --blur-radius 1.8`；
  - 保留细结构：`--splat-radius 7 --fill-radius 8 --blur-radius 1.0`；
  - 诊断深度断裂：`--edge-mode depth`。

### 新判断

自然化渲染应优先作为 VLM 输入，而不是人工诊断图。给 VLM 的图像应该尽量减少内部裂纹、伪纹理和点球颗粒；用于诊断回投可靠性的图像可以单独通过 `confidence.png`、`source.npy` 和 `edge-mode depth` 查看。也就是说，VLM 图和诊断图需要分工，而不是一张图同时承担所有功能。

### 本地检查

- `python -m py_compile MultiEEAffordance\experiments\natural_surface_vlm\render_natural_surface_views.py MultiEEAffordance\experiments\natural_surface_vlm\project_natural_masks_to_3d.py` 通过。

### 下一步

- 在服务器上重新生成 `vlm_pilot_005` 的自然化视图，优先尝试 `--splat-radius 10 --fill-radius 12 --blur-radius 1.6 --edge-mode silhouette --smooth`。
- 若 handle 被糊粗或与主体粘连，降低到 `--splat-radius 7 --fill-radius 8 --blur-radius 1.0`。
- 如果自然化图仍不足以让 VLM 稳定识别目标部件，再考虑加入“局部目标放大 crop 图”作为 VLM 的第二输入，而不是继续盲目增大 splat 或 fill。

---

## 2026-05-25 自然化渲染加入相邻点中点补采样

### 完成内容

- 根据“如果点云两个点相邻达到一定距离，就在中间加一个点让点连起来”的新想法，更新 `experiments/natural_surface_vlm/render_natural_surface_views.py`。
- 新增 render-only midpoint densification：
  - `--densify-midpoints`：开启渲染前补中点；
  - `--densify-threshold-multiplier`：自动阈值 = 原始点云中位最近邻距离 × 倍率；
  - `--densify-max-neighbors`：限制每个原始点最多连接的邻居数；
  - `--densify-distance`：允许手动指定归一化坐标下的连接距离；
  - `--densify-max-midpoints`：限制最多补点数量，避免渲染点数失控。
- 关键约束：中点只用于渲染，不改变原始点云的 N。每个中点会映射回某个原始端点索引，因此 2D mask 回投后仍然得到原始点云上的 `[N,4]` mask。
- 更新 `experiments/natural_surface_vlm/README.md`，加入 densify 推荐命令和调参建议。

### 新判断

中点补采样是比 mesh 重建更保守的连续化方法：它不会构造完整三角面，也不会把孔洞直接封死，只是在局部邻近点之间增加视觉连续性。它适合 bag handle、scissors ring、mug handle 等由稀疏点构成的细结构，但必须控制连接阈值，避免把本不相连的主体边缘、孔洞两侧或不同部件错误连起来。

### 本地检查

- `python -m py_compile MultiEEAffordance\experiments\natural_surface_vlm\render_natural_surface_views.py MultiEEAffordance\experiments\natural_surface_vlm\project_natural_masks_to_3d.py` 通过。

### 下一步

- 在服务器上对 `vlm_pilot_005` 重新生成自然化图，优先使用 `--densify-midpoints --densify-threshold-multiplier 2.2 --densify-max-neighbors 3`。
- 若 handle 仍不连续，提高 `--densify-threshold-multiplier` 到 2.5 或 3.0；若出现错误粘连，降低到 1.6 并把 `--densify-max-neighbors` 设为 2。

---

## 2026-05-25 自然化渲染 VLM 独立 Probe 测试脚本

### 完成内容

- 新增 `experiments/natural_surface_vlm/run_natural_vlm_probe.py`，用于独立测试 Qwen3-VL 是否能从自然化点云渲染图中识别任务和执行器所需的目标区域。
- 该脚本不改动 v3 pipeline，不写入 `processed/vlm_candidate_v3/`，只在 `processed/natural_surface_vlm/vlm_probe/` 下生成测试结果。
- Probe 分成两类能力测试：
  - semantic：判断 VLM 是否理解当前 object / task / executor 下应标注哪个功能部件，并输出 target / reject / uncertain；
  - localize：在同一张自然化图上让 VLM 给出粗 box / positive point，仅用于判断定位趋势。
- 输出内容包括：
  - `*_semantic.json`：每个视角的语义部件判断；
  - `*_localization.json`：每个视角的粗定位；
  - `*_probe_overlay.png`：将粗定位画回自然化图；
  - `combined_probe_summary.json`：多视角汇总；
  - `index.html`：人工浏览查看的汇总页面。
- 更新 `experiments/natural_surface_vlm/README.md`，补充独立 probe 的服务器运行命令和结果查看方式。

### 新判断

自然化渲染是否有价值，应该先分开评估两件事：第一，VLM 是否能看懂语义目标部件；第二，VLM 是否能给出足够准确的空间定位。若 semantic 正确但 localization 不准，说明自然化图已经改善了语义理解，但仍需要 GroundingDINO/Florence-2/SAM2 或人工候选修正；若 semantic 都不稳定，则说明继续优化渲染图的必要性更高。

### 本地检查

- `python -m py_compile MultiEEAffordance\experiments\natural_surface_vlm\render_natural_surface_views.py MultiEEAffordance\experiments\natural_surface_vlm\project_natural_masks_to_3d.py MultiEEAffordance\experiments\natural_surface_vlm\run_natural_vlm_probe.py` 通过。

### 下一步

- 在服务器端对 `vlm_pilot_005` 运行 `run_natural_vlm_probe.py`，重点查看 `combined_probe_summary.json` 和 `index.html`。
- 如果 `Bag / lift_carry / hook` 的 target 稳定指向 `bag handle / handle loop / handle inner rim`，说明自然化渲染对 VLM 语义理解有效。
- 如果 overlay 的 box/points 仍然覆盖 bag body，则下一步不应直接接 SAM2，而应先尝试更强的 grounding 模型或增加局部 crop 输入。

---

## 2026-05-25 自然化 Probe 定位结果分析与前景吸附修正

### 完成内容

- 根据 `vlm_pilot_005 / Bag / lift_carry / hook` 的自然化 probe 输出，确认 Qwen3-VL 在语义层面已经成功：
  - 能识别 `bag body` 和 `top handle`；
  - 将 `top handle` 判为 hook 目标正例；
  - 将 `bag body panel / flat bag surface / ordinary side edge` 判为 reject；
  - 能正确解释 hook 的进入、挂接和提拉约束机制。
- 同时确认粗定位仍存在偏差：
  - box 能大致框到上方 handle 区域，但偏大且包含背景；
  - positive/negative points 有部分落在非目标位置；
  - Qwen3-VL 输出的 `confidence=0.0` 不适合作为可靠置信度，需要更多依赖人工/规则诊断。
- 更新 `experiments/natural_surface_vlm/run_natural_vlm_probe.py`：
  - 新增定位后处理，默认开启；
  - 使用自然化渲染的 `point_index/confidence/source` 将 VLM box/point 吸附到可回投前景；
  - 当语义目标包含 `handle/loop/ring/hole/top/upper` 时，优先裁剪到主体上方的 foreground 结构，减少 box 覆盖 bag body 的问题；
  - 新增参数 `--no-refine-localization`、`--refine-min-confidence`、`--refine-snap-radius`、`--upper-margin`。
- 更新 `experiments/natural_surface_vlm/README.md`，补充 refined probe 的运行命令和说明。

### 新判断

自然化渲染已经显著改善了 VLM 的语义识别能力，但 Qwen3-VL 直接输出像素级定位仍不能直接进入 SAM2 或 3D 回投。更合理的路线是：VLM 负责判断目标部件和 reject 部件；定位部分必须经过前景吸附、上方结构约束、Grounding/SAM2 或人工候选检查。也就是说，当前证据支持“自然化图作为 VLM 语义输入”，但还不支持“Qwen3-VL 坐标直接作为标注”。

### 本地检查

- `python -m py_compile MultiEEAffordance\experiments\natural_surface_vlm\run_natural_vlm_probe.py` 通过。

### 下一步

- 在服务器端重新运行 `run_natural_vlm_probe.py`，查看 refined 后的 `*_probe_overlay.png` 是否更贴近 top handle。
- 如果 refined overlay 足够贴近 handle，可进入下一步实验：把 refined box/points 交给 SAM2，再用 `project_natural_masks_to_3d.py` 回投为 3D candidate mask。
- 如果 refined 后仍偏离目标，优先尝试局部 crop 输入或专门的 grounding 模型，而不是直接接入主 pipeline。
## 2026-05-25 大规模人工审查入口与已审查数据集可视化

### 完成内容

- 将 v3 pipeline 的默认任务策略收敛为 `pick_up / open_pull / press_push`，默认不再把 `lift_carry` 放入新一批候选生成和人工审查流程。
- 更新 `tools/run_v3_pipeline.py`：
  - 新增 `--pilot-csv`、`--include-tasks`、`--exclude-tasks`、`--filtered-pilot-csv`；
  - 默认写出过滤后的 pilot CSV，并把它传给 views / plan / ground / project / grow / render / build 各阶段；
  - run manifest 中记录任务过滤策略和过滤前后行数。
- 更新 `tools/build_v3_candidate_masks.py`，支持独立运行时按任务过滤，默认同样排除 `lift_carry`。
- 新增 `tools/build_large_scale_review_queue.py`：
  - 从 `samples_checked_v0_1.jsonl` 展开 channel-level 审查队列；
  - 每行对应一个 `sample_id + task + executor`；
  - 默认只保留三类任务，并可按 `feasible` executor 和 `needs_review` 样本生成第一批大规模审核 CSV。
- 已在本地生成第一版大规模审查队列：
  - `processed/metadata/v3_large_scale_review_queue_v0_1.csv`
  - `processed/metadata/v3_large_scale_review_queue_summary_v0_1.json`
  - 当前共 47 条 channel-level 审查单元，其中 `open_pull=17`、`pick_up=24`、`press_push=6`，并跳过 16 条 `lift_carry`。
- 新增 `tools/build_reviewed_dataset_release.py`：
  - 从人工点级审查输出中收集 `checked/verified` 样本；
  - 默认过滤掉 `lift_carry`；
  - 输出 `reviewed_dataset_v0_1.jsonl`、summary 和 split 文件，用于形成第一版人工审核数据集。
- 新增只读系统 `tools/serve_reviewed_dataset_viewer.py`：
  - 用于可视化已经完成人工审查的数据集；
  - 支持样本列表、任务/执行器/类别统计、点云旋转缩放、目标执行器正样本高亮、全部通道显示切换；
  - 与审查系统统一为蓝白配色。
- 更新 `tools/serve_v2_annotation_app.py` 的主视觉风格：
  - 审查系统从红/深灰主色改为蓝白主色；
  - 正样本点默认蓝色显示；
  - 删除模式仍保留红色 brush 提示，避免误操作。
- 新增说明文档 `docs/大规模人工审查与已审查数据集可视化.md`，记录从队列生成、v3 pipeline、网页审查、reviewed release 到只读可视化系统的完整命令。
- 更新 `.gitignore`，忽略 v3 candidate 中间产物和 v3 点级审查动态输出，避免大规模运行时把生成文件误提交。

### 新判断

`lift_carry` 不应从原始数据中硬删除，而应在新一版审查队列和数据集 release 层面默认排除。这样既能保持历史实验可复现，也能让第一版大规模人工审核的数据集任务定义更清晰。后续如果确实需要恢复 `lift_carry`，只需显式传入 `--include-tasks all` 或单独指定任务集合。

大规模人工审核不应继续手写 pilot CSV。更合理的入口是从已检查样本 JSONL 自动展开为 channel-level 队列，再交给 v3 pipeline 生成 top-k 候选，最后由人工在网页中选择候选组合并点级精修。已审查数据集也应有独立只读可视化系统，和审查系统职责分开：一个负责修改，一个负责展示和抽检。

### 本地检查

- `python -m py_compile MultiEEAffordance\tools\run_v3_pipeline.py MultiEEAffordance\tools\build_v3_candidate_masks.py MultiEEAffordance\tools\build_large_scale_review_queue.py MultiEEAffordance\tools\build_reviewed_dataset_release.py MultiEEAffordance\tools\serve_reviewed_dataset_viewer.py MultiEEAffordance\tools\serve_v2_annotation_app.py` 通过。
- `python MultiEEAffordance\tools\run_v3_pipeline.py --dataset-root MultiEEAffordance --stages views,plan,ground,project,grow,render,build --limit 3 --dry-run --overwrite` 通过，确认各阶段都读取过滤后的 pilot CSV。
- `python MultiEEAffordance\tools\build_large_scale_review_queue.py --dataset-root MultiEEAffordance --overwrite` 通过，生成 47 条第一版大规模审查队列。

### 下一步

- 在服务器端用 `v3_large_scale_review_queue_v0_1.csv` 作为 `--pilot-csv` 跑 v3 pipeline，先用小批量确认候选质量，再去掉 `--limit` 扩大规模。
- 审查系统建议显式写入 v3 输出路径：`v3_point_level_review_records.jsonl`、`v3_manual_refined_samples_v0_1.jsonl` 和 `processed/vlm_candidate_v3/manual_refined_masks/`。
- 第一批人工审查完成后运行 `build_reviewed_dataset_release.py`，再用 `serve_reviewed_dataset_viewer.py` 做只读抽检。

---

## 2026-05-25 v3 大规模队列、空标签审查与物体分层 UI 升级

### 完成内容

- 升级 `tools/build_large_scale_review_queue.py`：
  - 默认只保留 `pick_up / open_pull / press_push`，继续排除 `lift_carry`；
  - 增加物体-任务常识过滤，明显不匹配的组合不进入队列；
  - 默认按所有执行器展开，同一个物体任务下不可行的执行器不会被丢弃，而是写成 `empty_review_required`，交给人工确认空标签；
  - 新增 `review_mode`、`object_task_feasible`、`executor_feasible`、`negative_reason` 字段。
- 升级 `tools/run_v3_pipeline.py`：
  - 自动把普通候选生成行和空标签审查行分流；
  - 空标签行跳过 VLM/候选生成，只在 build 阶段写入 0 positive 样本；
  - 新增 `coverage` stage，可串联 VLM coverage check。
- 升级 v3 候选生成和构建：
  - `grow_v3_reject_aware_candidates.py` 增加 source weak mask fallback 候选，减少“候选太少导致全靠人工补”的情况；
  - `build_v3_candidate_masks.py` 支持 `--empty-pilot-csv`，为不可行执行器生成待确认空标签样本。
- 升级 VLM 阶段可观测性：
  - `run_v3_semantic_part_planner.py`、`run_v3_target_reject_grounding.py`、`run_vlm_coverage_check_v2.py` 增加行数、预计 view call 数和进度条。
- 升级审查系统和已审查数据集可视化系统：
  - 左侧列表改为按物体分组，物体下展示不同任务/执行器变体；
  - 列表按 pending/checked 分区；
  - 当前样本页面顶部新增明显任务横幅，突出显示 `object / task / executor / review_mode`；
  - 灰色背景点加深，便于看清物体主体和候选区域关系；
  - 空标签样本进入页面时自动提供 `confirm_empty` 决策选项。
- 更新 `docs/大规模人工审查与已审查数据集可视化.md`，补充新版服务器运行命令和 47 条样本来源解释。

### 新判断

第一版大规模人工审查不应只保留“自动判断可行”的执行器，否则数据集只能说明“这些地方能标”，不能说明“同一物体任务下不同执行器的可行性差异”。因此新版队列把不可行执行器也作为空标签样本保留，人工确认后形成真正的负例/空例。VLM 阶段仍然只跑普通候选生成样本，避免把大量空标签样本送入慢速多视角推理。

此前只有 47 条，是因为队列来自小规模已检查样本，并且使用 `feasible + needs_review` 过滤。新版在相同小源上测试会展开为更多 channel-level 行，其中一部分是 `empty_review_required`；实际大规模数据应先从 `raw/3d_affordancenet/full-shape.zip` 转出更大的 `samples_v3_large_batch_v0_1.jsonl`，再生成审查队列。

### 本地检查

- `python -m py_compile MultiEEAffordance\tools\build_large_scale_review_queue.py MultiEEAffordance\tools\run_v3_pipeline.py MultiEEAffordance\tools\run_v3_semantic_part_planner.py MultiEEAffordance\tools\run_v3_target_reject_grounding.py MultiEEAffordance\tools\run_vlm_coverage_check_v2.py MultiEEAffordance\tools\grow_v3_reject_aware_candidates.py MultiEEAffordance\tools\build_v3_candidate_masks.py MultiEEAffordance\tools\serve_v2_annotation_app.py MultiEEAffordance\tools\serve_reviewed_dataset_viewer.py` 通过。
- 使用临时输出测试新版队列生成通过：当前小规模源可生成 112 条 channel-level 队列，其中 `review=52`、`empty_review_required=60`，并按策略跳过 `lift_carry` 和明显不匹配的物体-任务组合。

### 下一步

- 在服务器端按文档命令先用 `--limit 300` 生成第一批 v3 大规模队列，确认样本分布和空标签比例。
- 跑 `run_v3_pipeline.py --stages views,plan,ground,project,grow,render,coverage,build`，观察进度条和 coverage 输出。
- 用审查系统优先检查：空标签确认是否清晰、source weak mask fallback 是否显著减少人工补点、物体分组和任务/执行器切换是否比旧列表更快。

---

## 2026-05-25 v3 自然化渲染与 part3d 候选主链路接入

### 完成内容

- 将 `experiments/natural_surface_vlm/render_natural_surface_views.py` 从独立实验能力接入正式 v3：
  - 新增正式入口 `tools/render_natural_surface_views.py`；
  - 默认输出到 `processed/vlm_candidate_v3/natural_renders/`；
  - 每个视角输出目录固定为 `<sample_id>/<view>/natural_render.png`、`point_index.npy`、`exact_point_index.npy`、`confidence.npy`、`source.npy`、`panel.png`。
- 升级自然化渲染输出布局：
  - 不再使用旧的 `yaw000_elev20_natural.png` 平铺命名；
  - `view_manifest.json` 仍记录每个视角的完整路径；
  - render-only midpoint、splat 和 fill 像素都只回指原始点云索引，不改变原始 `N`。
- 新增 `tools/propose_v3_part_candidates.py`：
  - 第一版提供 `natural_cc` 后端；
  - 基于自然化视图、前景、连通组件、upper/detached structure、thin/loop-like component、source weak mask 和几何候选生成 `[K,N]` 候选；
  - 预留 `sam3d` 和 `partslippp` adapter，但不把外部模型作为默认依赖。
- 升级 `tools/run_v3_pipeline.py`：
  - 新默认链路改为 `views,plan,part_propose,render,part_select,part_filter,build`；
  - 新增 `--candidate-source part3d|grounding`，默认 `part3d`；
  - 新增 `--view-renderer natural|dense|both`，默认 `natural`；
  - 旧 `ground -> project -> grow` 路径保留，可用 `--candidate-source grounding` 复现实验。
- 升级 VLM 候选选择和规则过滤：
  - `run_vlm_candidate_selection_v2.py` 增加 `--update-candidate-manifest`，可把 VLM 选择结果写回 `default_selected_candidates` 相关字段；
  - `filter_candidates_by_executor_rules.py` 增加 `--update-candidate-manifest` 和 `--manifest-selection-policy`，规则过滤后更新默认候选；
  - 两个阶段均增加进度条。
- 修复 `build_v3_candidate_masks.py` 的空样本和异常 metadata 鲁棒性：
  - 新增 `safe_dict(value)`；
  - `feasibility / label_source / negative_reason` 遇到字符串、列表或空值不再崩溃；
  - `build empty` 单条失败时生成 `failed_needs_review` 占位项，整批流程继续。
- 更新说明文档：
  - 新增 `docs/v3自然化渲染与part3d候选主链路说明.md`；
  - 更新 `experiments/natural_surface_vlm/README.md`，同步新的视角目录结构和正式 v3 运行命令。

### 推荐服务器命令

```bash
CUDA_VISIBLE_DEVICES=1,2 python MultiEEAffordance/tools/run_v3_pipeline.py \
  --dataset-root MultiEEAffordance \
  --config configs/qwen3vl_sam2_pilot.yaml \
  --pilot-csv processed/metadata/v3_large_scale_review_queue_v0_1.csv \
  --samples processed/metadata/samples_v3_large_batch_v0_1.jsonl \
  --include-tasks pick_up,open_pull,press_push \
  --exclude-tasks lift_carry \
  --candidate-source part3d \
  --view-renderer natural \
  --stages views,plan,part_propose,render,part_select,part_filter,build \
  --allow-empty \
  --overwrite
```

旧 grounding fallback：

```bash
CUDA_VISIBLE_DEVICES=1,2 python MultiEEAffordance/tools/run_v3_pipeline.py \
  --dataset-root MultiEEAffordance \
  --config configs/qwen3vl_sam2_pilot.yaml \
  --pilot-csv processed/metadata/v3_large_scale_review_queue_v0_1.csv \
  --samples processed/metadata/samples_v3_large_batch_v0_1.jsonl \
  --candidate-source grounding \
  --view-renderer natural \
  --stages views,plan,ground,project,grow,render,coverage,build \
  --allow-empty \
  --overwrite
```

### 本地检查

- `python -m py_compile` 覆盖新增和修改的 v3/natural/selection/filter/build 脚本，通过。
- 自然化渲染 smoke test 通过：
  - `point_index.shape == (768, 768)`；
  - 有效索引全部 `< N`；
  - `source` 只包含 `{0,1,2}`；
  - `confidence` 范围为 `[0,1]`。
- `part_propose` smoke test 通过：
  - 可生成 v3 兼容 `candidate_manifest.json`；
  - `candidate_masks.shape == [K,N]`；
  - 默认不把自动候选直接当作 GT。

### 新判断

新链路不再让 VLM 直接承担像素级定位职责。VLM 更适合做“目标部件/拒绝部件/候选 ID 是否适合当前任务和执行器”的判断；点级候选应先由自然化前景、图像连通组件、几何结构和弱标签来源生成，再由 VLM/规则/人工逐级收敛。这样可以减少 Florence/box grounding 把剪刀刃、主体边界等普通轮廓误当成 hook 正例的问题。

---

## 2026-05-25 撤销自然化渲染正式模块，收敛为部件分割候选接口

### 完成内容

- 从正式 v3 pipeline 中移除自然化渲染模块：
  - 删除 `tools/render_natural_surface_views.py`；
  - 删除 `experiments/natural_surface_vlm/` 下的自然化渲染、回投和 probe 脚本；
  - 删除自然化渲染主链路说明文档。
- 更新 `tools/run_v3_pipeline.py`：
  - 移除 `--view-renderer natural|dense|both`；
  - 移除 `--natural-renders-root`；
  - `views` 阶段统一使用原有 `render_vlm_friendly_views.py`；
  - 新候选源改为 `--candidate-source partseg`，旧 grounding 路径仍可用 `--candidate-source grounding`。
- 更新 `tools/propose_v3_part_candidates.py`：
  - 正式定位为“部件分割候选接口”；
  - 默认 backend 改为 `geometry`，作为本地可运行 fallback；
  - 保留 `partslippp` backend 参数作为后续 PartSLIP++ adapter 入口；
  - 输出协议仍为 `candidate_manifest.json` 与 `candidates.npz`，其中候选 mask 形状为 `[K,N]`。
- 更新 VLM/overlay 读取图像逻辑：
  - `run_v3_semantic_part_planner.py` 不再优先读取 `natural_render_path`；
  - `run_v3_target_reject_grounding.py` 和 `render_candidate_overlays_v2.py` 不再依赖自然化视图字段。
- 新增说明文档 `docs/v3部件分割候选与PartSLIP++接入说明.md`，记录小批测试、300 条放大、全量运行和 PartSLIP++ adapter 输出协议。

### 新判断

自然化渲染虽然曾用于探索提升 VLM 可读性，但它会引入新的视觉代理、回投可信度和填补像素管理问题，反而让主 pipeline 复杂化。当前更合理的方向是把候选生成层收敛为“真实点云上的部件分割候选”，让 PartSLIP++ 等 3D 部件分割模型后续直接产出 `[K,N]` 候选，再由 VLM 只做候选 ID 选择和机制判断。

---

## 2026-05-26 PartSLIP++ 部件分割候选 adapter 接入

### 完成内容

- 升级 `tools/propose_v3_part_candidates.py`：
  - `--backend partslippp` 不再是纯占位；
  - 新增 `--partslippp-root`、`--partslippp-path`、`--partslippp-fallback`；
  - 可读取外部 PartSLIP++ `.npz/.json` 结果，并转换为 v3 标准 `candidate_manifest.json` 与 `candidates.npz`；
  - 支持 `candidate_masks/masks/part_masks`、`semantic_seg`、`instance_seg`、`point_indices`、`label_names`、`scores` 等字段；
  - 如果 PartSLIP++ 只对部分点输出结果，可用 `point_indices` 回填到原始 `[N]` 点云。
- 升级 `tools/run_v3_pipeline.py`：
  - 透传 `--partslippp-root`、`--partslippp-path`、`--partslippp-fallback` 到 `part_propose` 阶段；
  - 当时用于测试 PartSLIP++ adapter 的命令为 `--candidate-source partseg --part-proposal-backend partslippp`；当前主线已切换为 `high_recall`。
- 升级 `tools/filter_candidates_by_executor_rules.py`：
  - 对“VLM 已选中的 PartSLIP++ 部件候选”增加保守正向加权；
  - 仍保留尺寸、执行器机制和人工审查约束，避免 PartSLIP++ 大主体候选直接成为 GT。
- 更新文档：
  - 重写 `docs/v3部件分割候选与PartSLIP++接入说明.md`，详细说明服务器下载、安装、输出格式、小批测试、全量运行和常见问题；
  - 新增 `docs/README.md`，按“当前主线 / 服务器运行手册 / 数据转换 / 设计汇报 / 历史实验”重新整理文档入口。

### 本地检查

- `python -m py_compile MultiEEAffordance\tools\propose_v3_part_candidates.py MultiEEAffordance\tools\run_v3_pipeline.py` 通过。
- 使用临时 `segments.npz` smoke test 通过：
  - 输入 `semantic_seg + label_names`；
  - 输出 2 个 PartSLIP++ 候选；
  - `candidate_manifest.json` 中候选名称正确映射为外部部件名。

### 新判断

PartSLIP++ 不应直接写入最终 `[N,4]` affordance mask。它的最合适位置是“候选部件区域生成器”：先把物体拆成语义部件候选，再让 VLM 根据任务和执行器机制选择哪些候选可用，最后由规则和人工审查收敛。这样既能利用 PartSLIP++ 的部件分割能力，也能保留 Multi-EE 数据集对不同执行器差异的判别能力。

---

## 2026-05-26 PartSLIP++ 服务器路径统一

### 完成内容

- 将 PartSLIP++ 服务器端固定工作区统一为：
  - `/home/lzq/Multi-EE-3DAG/MultiEEAffordance/external/partslippp`
- 将 v3 pipeline 默认读取 PartSLIP++ 标准化输出的目录改为：
  - `external/partslippp/outputs`
- 更新 `docs/v3部件分割候选与PartSLIP++接入说明.md`：
  - 按该固定路径重写服务器操作步骤；
  - 明确区分 PartSLIP++ 代码、checkpoint/data、以及给 v3 adapter 读取的 `segments.npz` 输出；
  - 更新小批测试和全量运行命令中的 `--partslippp-root`。

### 新判断

PartSLIP++ 自身仍应保留独立 conda 环境；Multi-EE 主环境只读取 PartSLIP++ 导出的标准化 `segments.npz/json`。这样可以避免 PyTorch3D、GLIP、cut-pursuit 与当前 VLM/SAM2 环境发生依赖冲突。

---

## 2026-05-26 放弃 PartSLIP++ 主线，切换自研高召回 3D 候选生成器

### 背景

PartSLIP++ 环境依赖较重，服务器安装过程容易受到 Python/CUDA/PyTorch3D/GLIP/cut-pursuit 和磁盘空间影响。更重要的是，当前标注任务的核心需求不是直接获得一个外部模型的单一分割结果，而是为人工审查提供高召回、可解释、可组合的 3D part candidates。

### 完成内容

- 将 `tools/propose_v3_part_candidates.py` 新增并默认切换为 `--backend high_recall`：
  - 自研、无外部模型依赖；
  - 允许候选重叠；
  - 每个候选组保留 top-k；
  - 低置信候选标记 `need_review=true`；
  - 保留 `small_part`、`visual_partial_component`、`coarse_partition`、`loop_handle_lip`、`smooth_panel_surface`、`paired_parts` 等候选族。
- 将 `tools/run_v3_pipeline.py` 默认 `--part-proposal-backend` 改为 `high_recall`：
  - 新增 `--proposal-max-candidates`，用于生成阶段候选池大小；
  - 保留 `--max-candidates` 只控制 overlay/VLM 展示数量；
  - 新增 `--part-top-k`、`--dedupe-iou`、`--small-part-max-fraction`。
- 更新 `tools/filter_candidates_by_executor_rules.py`：
  - 识别新的 high-recall 候选 family；
  - 对 hook/gripper/dexterous_hand/suction 的规则评分适配新 family；
  - 继续要求 VLM 支持后才自动 accept。
- 新增 `docs/v3自研高召回3D候选生成器说明.md`：
  - 记录设计原则、小批测试、正式运行和调参建议。
- 更新 `docs/README.md`：
  - 当前主线文档切换为自研高召回候选生成器；
  - PartSLIP++ 文档移入历史实验与参考。

### 新判断

候选生成器的目标不是替代人工，而是显著减少人工从零点点云开始标注的工作量。当前应优先保证 recall：宁可多给候选、让 VLM/规则/人工逐级收敛，也不要在候选生成阶段因为低置信、小部件、部分可见或重叠就提前删除。

---

## 2026-05-26 同步 high_recall 命令与文档分类

### 完成内容

- 更新 `docs/README.md`：
  - 在文档入口顶部加入当前推荐命令速查；
  - 明确当前主线为 `--candidate-source partseg --part-proposal-backend high_recall`；
  - 将 PartSLIP++、旧 VLM 多视角和 target/reject seed-growth 文档归入历史实验与参考。
- 重写 `docs/大规模人工审查与已审查数据集可视化.md`：
  - 按“生成弱标签样本池 -> 生成 channel-level 队列 -> 小批候选 overlay -> 完整 v3 pipeline -> 人工审查 -> reviewed dataset release -> 只读可视化”重新组织；
  - 将推荐 pipeline 命令更新为 `views,plan,part_propose,render,part_select,part_filter,build`；
  - 增加只跑 `views,part_propose,render` 的小批候选检查命令；
  - 将旧 `ground,project,grow,coverage` 命令标记为历史 fallback。
- 更新 `docs/V3语义目标拒绝候选Pipeline设计与汇报.md`：
  - 在顶部标注该文档为历史设计记录；
  - 将旧命令改为历史复现实验命令；
  - 补充当前 `high_recall` 主线命令，避免后续误用旧链路。

### 新判断

文档入口必须优先回答“现在该跑哪条命令”。旧方案仍有研究记录价值，但不能继续放在推荐命令位置，否则很容易把 `candidate_count=0` 的旧 target/reject seed-growth 输出误认为当前 high-recall 主线失败。

---

## 2026-05-27 纯人工审查输入改为数据盘镜像存储

### 完成内容

- 更新 `tools/run_v3_pipeline.py`：
  - 新增 `--data-storage-root`，用于把 v3 中间候选、fused mask、review input samples 和 run manifest 写到 `/home/lzq/data/MultiEEAffordance/`；
  - 新增 `--v3-output-root`、`--metadata-root`、`--review-output-samples`、`--review-summary-json`、`--review-split-dir`；
  - `--stages part_propose,build` 不再隐式触发 `views`，适配当前“纯规则候选 + 人工审查”的轻量流程；
  - `part_propose/build/render/part_select/part_filter/coverage/visualize` 的默认 v3 子目录改为从统一 v3 output root 派生。
- 更新 `tools/propose_v3_part_candidates.py`：
  - 新增 `--metadata-root`，候选 manifest 中的 `candidate_npz` 可以写成相对数据镜像根的 portable path。
- 更新 `tools/build_v3_candidate_masks.py`：
  - 新增 `--metadata-root`；
  - 生成的 `v3_candidate_samples_v0_1.jsonl` 中，`multi_channel_mask_path`、`candidate_manifest`、summary 路径可写成 `processed/...`，便于审查者本地解压后直接运行网页。
- 新增 `tools/package_review_inputs.py`：
  - 从 `/home/lzq/data/MultiEEAffordance` 和当前项目 `MultiEEAffordance` 两个位置收集 reviewer samples 引用到的点云、mask、candidate manifest、candidate npz；
  - 输出可直接在项目根目录解压的 `tar.gz` 审查输入包。
- 更新 `docs/维护者协作标注README.md`：
  - 增加 `/home/lzq/data` 数据盘存储约定；
  - 将当前推荐 pipeline 命令改为 `--stages part_propose,build --data-storage-root /home/lzq/data`；
  - 增加 `package_review_inputs.py` 的打包命令。

### 验证

- `python -m py_compile MultiEEAffordance/tools/package_review_inputs.py MultiEEAffordance/tools/run_v3_pipeline.py MultiEEAffordance/tools/propose_v3_part_candidates.py MultiEEAffordance/tools/build_v3_candidate_masks.py` 通过。
- `run_v3_pipeline.py --stages part_propose,build --data-storage-root /home/lzq/data --dry-run` 通过，确认只生成 `part_propose` 和 `build` 两个阶段命令。

### 新判断

当前标注生产线应把“审查输入数据包”视为可迁移产物，而不是仓库内部临时文件。服务器端使用 `/home/lzq/data/MultiEEAffordance` 作为数据镜像根；审查者本地把包解压到项目根目录后仍使用 `MultiEEAffordance/processed/...` 相对路径。人工审查输出继续写入项目下 `processed/annotation_batches/v0_1/`，避免混入候选生成中间目录。

---

## 2026-05-26 双人协作标注 v0.1 版本冻结规划

### 完成内容

- 明确当前轻量自动候选 + 人工点级审查版本作为第一版协作标注工具：
  - 稳定标注分支：`annotation/mvp-v0.1`
  - 稳定标注标签：`annotation-mvp-v0.1`
  - 候选生成研发分支：`dev/high-recall-candidate-v0.2`
- 新增 `docs/双人协作标注README.md`：
  - 使用 `reviewer_a` / `reviewer_b` 中性身份；
  - 说明共享服务器模式、样本按 `object_id` 分配、两个端口分别启动审查系统；
  - 约定两位审查者分别写入独立 review JSONL、refined samples JSONL 和 refined mask 目录；
  - 记录每日交付、保存检查、合并 release 和 conflict review 规则。
- 新增 `docs/标注版本冻结与研发分支说明.md`：
  - 说明为什么人工标注开始后不能继续混改候选生成器；
  - 说明哪些改动可以进入稳定标注分支，哪些应进入候选生成研发分支；
  - 给出创建分支、tag 和推送到 GitHub 的建议命令。
- 更新 `docs/README.md`：
  - 在文档入口加入当前双人协作标注版本说明；
  - 将双人协作 README 和版本冻结说明放入当前主线文档；
  - 调整推荐阅读顺序。
- 更新 `docs/大规模人工审查与已审查数据集可视化.md`：
  - 增加 `processed/annotation_batches/v0_1/` 批次目录结构；
  - 增加 `reviewer_a` / `reviewer_b` 两套启动命令；
  - 增加两份 refined samples 合并后再打包 reviewed dataset 的流程。
- 更新 `docs/V3语义目标拒绝候选Pipeline设计与汇报.md` 和 `docs/v3自研高召回3D候选生成器说明.md`：
  - 标注当前 v3 进入稳定协作审查版本；
  - 明确候选生成器后续改进不阻塞当前标注启动。
- 更新 `.gitignore`：
  - 忽略 `MultiEEAffordance/processed/annotation_batches/`，避免将人工 refined mask 和批量 review log 误提交到 Git。
- 将阅读材料文档重命名为 `协作者阅读论文清单.md`，保持当前协作流程的中性身份命名。
- 进一步扩展 `docs/双人协作标注README.md` 和 `docs/标注版本冻结与研发分支说明.md`：
  - 增加角色分工、完整协作流程、维护者启动前检查、审查页面校验、空 mask 判断、问题样本记录、每日备份、冲突检测、只读 viewer 抽检和常见问题；
  - 增加稳定分支创建前检查、hotfix 流程、批次目录建议、v0.2 升级条件和维护者/审查者视角执行顺序。

### 新判断

当前最重要的是把“稳定标注生产线”和“候选生成器研发线”拆开。标注分支只保证审查系统可用、输出可保存、release 可打包；候选生成质量改进在研发分支继续推进。这样两位审查者可以马上开始产出数据，而后续算法迭代不会污染已经完成的 v0.1 人工审查结果。

---
## 2026-05-29 五任务 taxonomy 与双人协作标注流程整理

### 完成内容

- 建立统一任务 taxonomy：`MultiEEAffordance/utils/task_taxonomy.py`。
  - 新五任务：`lift/open/pull/press/push`。
  - 旧任务：`pick_up/lift_carry/open_pull/press_push`。
  - 映射：`pick_up -> lift`，`open_pull -> open + pull`，`press_push -> press + push`，`lift_carry -> lift`。
  - 当前版本号：`v0_2_5tasks`。
- 保留旧任务候选生成兼容，不覆盖已经生成的 val / train_100pcat 候选输出。
- 新增五任务展开入口：`tools/expand_legacy_tasks_to_5tasks.py`。
- 将 reviewed dataset release、只读 viewer、HTML export、dataset checker 等入口更新为可识别五任务。
- 将打包脚本 `package_annotation_batches_from_samples.py` 对齐到五任务 reviewer 包：
  - 检查 `row_key` 和 `sample_id + task + executor` 唯一性；
  - 生成 `reviewer_a/reviewer_b` 独立 samples；
  - 打包点云、mask、candidate manifest、candidate npz；
  - 压缩包 README 改为当前 `serve_v2_annotation_app.py` 参数。
- 同步更新五任务相关文档：
  - `双人协作标注README_5tasks.md`
  - `维护者协作标注README_5tasks.md`
  - `维护者协作标注README_NEW.md`
  - `docs/README.md`
  - `README.md`
  - `大规模人工审查与已审查数据集可视化.md`
  - `异构末端执行器标注规范.md`
  - `V3语义目标拒绝候选Pipeline设计与汇报.md`
  - `v3自研高召回3D候选生成器说明.md`

### 新判断

旧任务候选数据仍然有价值，但只能作为人工标注 proposal。五任务数据集的真实标签必须来自 reviewer 在网页中按 `lift/open/pull/press/push` 语义保存的 refined mask。后续任何候选生成改进都应另开批次，不应覆盖已经进入人工审查的旧候选输出。

---

## 2026-06-01 数据服务器统一与 PartNet-Mobility 转换入口

### 完成内容

- 明确数据存储服务器：
  - 服务器 IP：`10.24.1.11`。
  - 数据镜像根目录：`/home/lzq/data/MultiEEAffordance`。
  - 五任务下采样、网页审查系统和人工 refined 输出统一在该服务器执行和保存。
  - GitHub 只保存代码、文档和小型元数据，不提交大规模 `.npy/.npz` 中间文件。
- 明确第一版大规模数据目标：
  - 3D AffordanceNet：`8000+` 五任务审查样本行。
  - PartNet-Mobility：`4000+` 五任务审查样本行。
  - 合计：约 `1.2w` 样本行。
  - PartNet-Mobility raw object 数和任务展开后的样本行数分开统计。
- 新增 `tools/convert_partnet_mobility.py`：
  - 支持安全解压 `partnet-mobility-v0.zip`。
  - 扫描 `mobility.urdf`、`meta.json` 和 visual OBJ mesh。
  - 对每个 URDF link 做 surface sampling，并给小部件保留最小采样预算。
  - 输出标准化 `[N,3]` 点云。
  - 输出 `[K,N]` link-level 部件候选 `parts.npz`。
  - 输出 `candidate_manifest.json` 和 object-level `objects_manifest.jsonl`。
  - 所有 PartNet-Mobility link mask 明确标记为 `proposal_only=true`，不能直接作为 `[N,4]` 最终标签。
- 新增 `docs/PartNet-Mobility数据解压与格式转换.md`：
  - 记录复制 zip、解压、小批 smoke test、全量转换和检查命令。

### 新判断

PartNet-Mobility 的价值不在于直接提供执行器标签，而在于提供 articulated object 的结构化部件 proposal。后续应先利用 link、joint 和 movable part 信息生成五任务审查样本，再由规则层和人工审查形成最终 `[N,4]` mask。

---

## 2026-06-01 PartNet-Mobility 补充类别收敛

### 完成内容

- 将 `tools/convert_partnet_mobility.py` 的默认转换范围收敛为 21 个补充类别：
  - `Box/Bucket/Cabinet/Camera/CoffeeMachine/Dispenser/Kettle/Lighter/Mouse/Oven/Phone/Pliers/Remote/Safe/Stapler/Suitcase/Switch/Toaster/Toilet/WashingMachine/Window`。
- 保留命令行覆盖能力：
  - `--categories Box,Cabinet,Switch`：只转换指定类别。
  - `--categories all`：仅在诊断原始数据时显式转换全部类别。
- 将默认输出目录改为 `partnet_mobility_v0_supplement_21cat`，避免与历史全类别试跑结果混用。

### 新判断

PartNet-Mobility 当前是 3D AffordanceNet 的补充来源。转换阶段应优先控制类别边界，再生成 link-level proposal 和五任务审查样本；不应直接把原始 PartNet-Mobility 全类别并入第一版数据集。

---

## 2026-06-01 PartNet-Mobility 五任务审查链路补齐

### 完成内容

- 新增 `tools/build_partnet_5task_review_samples.py`：
  - 读取 PartNet-Mobility object-level manifest。
  - 按补充类别生成高召回 `lift/open/pull/press/push` 任务组合。
  - 为每个物体-任务组合生成四执行器审查行。
  - 生成初始全零 `[N,4]` mask。
  - 保留 URDF link-level `candidate_manifest.json` 和 `parts.npz` 作为网页人工选择 proposal。
  - 校验点云、candidate manifest 和 candidate npz 的路径与 shape。
- 更新 `tools/serve_v2_annotation_app.py`：
  - 当候选没有 VLM 投票或规则筛选元数据时，仍展示 manifest 中的前 `top-k` 个 proposal。
  - PartNet 候选优先展示 movable link。
  - 人工保存后，根据实际正例数量同步更新当前执行器的 `feasibility`、`label_source` 和 `negative_reason`。
- 更新 `tools/package_annotation_batches_from_samples.py`：
  - PartNet 原始 OBJ mesh 路径仅作为溯源信息，不复制到本地审查压缩包。
  - 避免产生无意义 missing-reference 警告和体积过大的 reviewer 包。
- 更新 `docs/PartNet-Mobility数据解压与格式转换.md`：
  - 增加转换、适配、双人分包、网页启动和 reviewed release 的完整命令。

### 新判断

PartNet-Mobility 不应复用 3D AffordanceNet 的旧任务候选生成链路。它已经具有 URDF link-level 部件 proposal，应直接生成五任务人工审查样本，再复用双人分包、网页审查和 reviewed release 脚本。

---

## 2026-06-01 独立五任务训练 Pipeline 初版

### 完成内容

- 新增独立目录 `MultiEEAffordance/training/`。
- 训练侧只接受五个独立任务：
  - `lift`
  - `open`
  - `pull`
  - `press`
  - `push`
- 训练侧明确拒绝旧任务：
  - `pick_up`
  - `lift_carry`
  - `open_pull`
  - `press_push`
- 新增 `training/prepare_training_dataset.py`：
  - 从 `reviewer_a/reviewer_b` 的人工 refined samples 读取标签。
  - 按 `object_id + task` 合并四类执行器通道。
  - 生成训练专用 canonical `[N,4]` mask。
  - 保留 `channel_supervision`，允许人工审查阶段先用部分通道试跑。
  - 正式 release 可用 `--min-reviewed-channels 4` 强制要求四通道完整。
  - 同一通道存在冲突时不自动覆盖，写入 `conflict_rows.json`。
  - 按 `object_id` deterministic split，避免同一物体泄漏到 train 和 test。
- 新增初版轻量模型 `TaskExecutorPointNet`：
  - 输入点云和独立五任务 ID。
  - 使用共享 point encoder、task embedding 和四类 executor query。
  - 输出点级 `[B,N,4]` mask logits。
  - 输出 object-task-level `[B,4]` feasibility logits。
- 新增 focal BCE、Dice、feasibility 和 executor relation loss。
- 新增训练、评估、配置和 synthetic smoke test 入口。
- 默认训练数据和 checkpoint 保存在数据服务器：

```text
服务器：10.24.1.11
数据根目录：/home/lzq/data/MultiEEAffordance
训练数据：processed/training/
训练输出：processed/training_runs/
```

### 新判断

训练 pipeline 可以部署在数据存储服务器上，但 GPU 训练的前提是 `10.24.1.11` 具备可用 GPU、驱动和 PyTorch CUDA 环境。无论是否具备 GPU，人工审查结果合并和训练 manifest 生成都应直接在数据存储服务器执行，避免复制大规模点云和 refined mask。

初版 `TaskExecutorPointNet` 的定位是验证训练闭环。正式投稿模型应在数据 schema 和 verified split 冻结后替换为 PointNeXt 或 Point Transformer 主干，并完成 cross-source、small-part 和 empty-mask 实验。

---

## 2026-06-01 AAAI 投稿排期冻结

### 完成内容

- 新增 `docs/AAAI投稿时间规划_2026-06-01.md`。
- 根据当前标注仍需约一周半的现实情况，将后续工作拆分为五条并行工作线：
  - 数据标注。
  - 数据质检与 release。
  - 模型开发与实验。
  - 论文写作。
  - 投稿材料准备。
- 截至 `2026-06-01`，AAAI 官方仅公布 AAAI-27 将于 `2027-02-16` 至 `2027-02-23` 在 Montréal 举行，尚未公布 AAAI-27 CFP 和截止日期。
- 当前以 AAAI-26 官方节奏作为保守参考，设置内部截止：
  - `2026-07-20`：摘要和主线冻结。
  - `2026-07-27`：正文候选版本冻结。
  - `2026-07-30`：补充材料和投稿文件冻结。
- 明确范围控制原则：
  - 当前不再扩张任务体系。
  - 当前不再持续增加网页功能。
  - 标注、数据 release、baseline、正式方法、核心实验和论文写作必须并行推进。

### 新判断

当前项目已经进入投稿收敛阶段。任何新增模块都必须能够回答明确论文问题，并通过独立实验说明价值；否则进入投稿后的研发清单，不进入当前主线。

---
