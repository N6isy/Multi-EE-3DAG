# 项目进度日志

> 文档状态：当前主线
> 当前主线：五任务人工标注完成，训练进入 PointNeXt + 执行器属性条件建模阶段。
> 最新入口：docs/README.md、training/README.md、五任务训练初版结果进度汇报.md。

## 2026-06-16

完成训练模块改造第一版：

```text
新增 training/backbones/
新增 training/model_factory.py
新增 training/executor_conditioning.py
新增 executor_specs_v0_1.json
新增 PointNeXt 和 executor attribute 消融配置
新增 smoke_test_backbone.py
新增 docs/ 文档入口和方法说明
```

保留旧 PointNet baseline 兼容：

```text
旧配置不写 model_type/backbone 时仍然构建 TaskExecutorPointNet
training/smoke_test.py 已通过
新工厂下 legacy PointNet 输出 [B,N,4] 和 [B,4]
```

环境检查：

```text
openpoints 顶层 import 已通过
PointNeXt forward 缺少 pointnet2_batch_cuda
当前机器 nvidia-smi 无法连接驱动，且未发现 nvcc
```

下一步：

```text
在 GPU/CUDA 环境编译 OpenPoints CUDA extension
运行 PointNeXt backbone smoke test
运行 pointnext_shared4_5tasks_debug.json
正式启动 PointNeXt 和执行器属性消融
```
